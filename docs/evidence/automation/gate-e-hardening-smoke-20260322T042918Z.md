# Gate E Hardening Smoke

Generated: 2026-03-22 04:29:19 UTC

## Output

```json
{
  "generated_at_utc": "2026-03-22T04:29:18.437Z",
  "staging_supabase_url": "https://madjklbsdwbtrqhpxmfs.supabase.co",
  "tenant_key": "ashtabulacity",
  "duplicate_submit_runtime": {
    "seed_incident": {
      "incident_id": "water_drain_issues:41.86370:-80.77179",
      "lat": 41.8637043175751,
      "lng": -80.771790070587,
      "issue_type": "sewer_backup"
    },
    "first_gate_call": {
      "status": 200,
      "body": {
        "allowed": true,
        "degraded": true,
        "reason": "exception",
        "error": "TypeError: admin.rpc(...).catch is not a function"
      }
    },
    "second_gate_call_same_idempotency": {
      "status": 200,
      "body": {
        "allowed": true,
        "duplicate": true,
        "remaining": 5,
        "remainingUnits": 18
      }
    },
    "first_report_insert": {
      "status": 201,
      "body": null
    },
    "second_report_insert_attempt": {
      "skipped": true,
      "reason": "duplicate idempotency should block second insert client-side"
    }
  },
  "static_hardening_evidence": {
    "client_idempotency_logic": [
      "99:const DOMAIN_SUBMIT_DEDUPE_WINDOW_MS = 60 * 1000;",
      "1211:  idempotencyKey = \"\",",
      "1233:        idempotency_key: String(idempotencyKey || \"\").trim() || null,",
      "11167:  const domainSubmitDedupRef = useRef(new Map());",
      "16029:    const dedupeMap = domainSubmitDedupRef.current;",
      "16031:      if (!k || !Number.isFinite(Number(ts)) || (nowTs - Number(ts)) > DOMAIN_SUBMIT_DEDUPE_WINDOW_MS) {",
      "16037:      if (prevTs > 0 && (nowTs - prevTs) < DOMAIN_SUBMIT_DEDUPE_WINDOW_MS) {",
      "16091:        idempotencyKey: submitKey,",
      "16479:        domainSubmitDedupRef.current.delete(submitKey);"
    ],
    "my_reports_identity_logic": [
      "870:    const email = normalizeEmail(parsed.reporter_email || parsed.actor_email || \"\") || null;",
      "871:    const phone = normalizePhone(parsed.reporter_phone || parsed.actor_phone || \"\") || null;",
      "939:        r.reporter_user_id ? `uid:${r.reporter_user_id}` :",
      "940:        (normalizeEmail(r.reporter_email) ? `email:${normalizeEmail(r.reporter_email)}` :",
      "941:         (normalizePhone(r.reporter_phone) ? `phone:${normalizePhone(r.reporter_phone)}` : null));",
      "971:  if (r?.reporter_user_id) return `uid:${r.reporter_user_id}`;",
      "972:  const email = normalizeEmail(r?.reporter_email);",
      "974:  const phone = normalizePhone(r?.reporter_phone);",
      "3052:        { swatch: markerSwatch(\"#111\", { ring: \"#2ecc71\", showGlyph: false }), label: \"Saved to My Reports (green outline)\" },",
      "3086:    { iconSrc: UI_ICON_SRC.account, label: \"Account\", desc: \"Open login/account menu and My Reports access.\" },",
      "3091:    { iconSrc: UI_ICON_SRC.bulk, label: \"Bulk Save (Streetlights)\", desc: \"Select and save multiple streetlights to My Reports.\" },",
      "3826:      const uid = (reportItem?.reporter_user_id || \"\").trim();",
      "3857:  }, [open, reportItem?.reporter_user_id]);",
      "3869:    (reportItem?.reporter_phone || \"\").trim() ||",
      "3873:    (reportItem?.reporter_email || \"\").trim() ||",
      "4171:                      {String(it?.reporter_name || \"\").trim() || String(it?.reporter_email || \"\").trim() || \"Unknown\"}",
      "4184:                          reporter_user_id: actorUserId || null,",
      "4186:                          reporter_email: actorEmail || null,",
      "4187:                          reporter_phone: actorPhone || null,",
      "4303:          const email = String(r?.reporter_email || \"\").toLowerCase();",
      "4305:          const phoneNorm = normalizePhone(r?.reporter_phone || \"\");",
      "4361:        <div style={{ fontSize: 16, fontWeight: 950 }}>My Reports</div>",
      "5178:  const isMyReportsModal = String(modalTitle || \"\").trim().toLowerCase() === \"my reports\";",
      "5600:        const email = String(r?.reporter_email || \"\").toLowerCase();",
      "5601:        const phoneNorm = normalizePhone(r?.reporter_phone || \"\");",
      "5727:            \"report_id,report_number,domain,incident_id,submitted_at,fixed_at,time_to_close_seconds,current_state,reporter_name,reporter_email,reporter_phone,notes,source\"",
      "5746:              `reporter_email.ilike.${like}`,",
      "5747:              `reporter_phone.ilike.${like}`,",
      "5885:          reporter_email: String(item?.row?.reporter_email || \"\"),",
      "5886:          reporter_phone: String(item?.row?.reporter_phone || \"\"),",
      "5932:            reporter_email: String(r?.reporter_email || \"\"),",
      "5933:            reporter_phone: String(r?.reporter_phone || \"\"),",
      "5957:            reporter_email: String(r?.reporter_email || \"\"),",
      "5958:            reporter_phone: String(r?.reporter_phone || \"\"),",
      "6046:          reporter_email: String(r?.reporter_email ?? \"\"),",
      "6047:          reporter_phone: String(r?.reporter_phone ?? \"\"),",
      "6174:        reporter_email: String(r?.reporter_email || \"\"),",
      "6175:        reporter_phone: String(r?.reporter_phone || \"\"),",
      "6234:          reporter_user_id: a?.actor_user_id || a?.reporter_user_id || null,",
      "6236:          reporter_email: String(a?.actor_email || a?.reporter_email || \"\").trim() || null,",
      "6237:          reporter_phone: String(a?.actor_phone || a?.reporter_phone || \"\").trim() || null,",
      "6244:          String(ev.reporter_user_id || \"\"),",
      "6252:        const fixedByUserId = fixAction?.actor_user_id || fixAction?.reporter_user_id || null;",
      "6281:          reporter_user_id: fixedByUserId,",
      "6283:          reporter_email: String(fixAction?.actor_email || fixAction?.reporter_email || \"\").trim() || fallbackEmail || null,",
      "6284:          reporter_phone: String(fixAction?.actor_phone || fixAction?.reporter_phone || \"\").trim() || fallbackPhone || null,",
      "6633:      \"reporter_email\",",
      "6634:      \"reporter_phone\",",
      "7924:                                  reporter_user_id: ev.reporter_user_id || null,",
      "7926:                                  reporter_email: ev.reporter_email || null,",
      "7927:                                  reporter_phone: ev.reporter_phone || null,",
      "7944:                              {String(ev.reporter_name || \"\").trim() || String(ev.reporter_email || \"\").trim() || \"Unknown\"}",
      "7976:                                  reporter_user_id: r.fixed_event.reporter_user_id || null,",
      "7978:                                  reporter_email: r.fixed_event.reporter_email || null,",
      "7979:                                  reporter_phone: r.fixed_event.reporter_phone || null,",
      "7996:                              {String(r.fixed_event.reporter_name || \"\").trim() || String(r.fixed_event.reporter_email || \"\").trim() || \"Unknown\"}",
      "8047:                                      reporter_user_id: detail.reporter_user_id || null,",
      "8049:                                      reporter_email: detail.reporter_email || null,",
      "8050:                                      reporter_phone: detail.reporter_phone || null,",
      "8067:                                  {String(detail.reporter_name || \"\").trim() || String(detail.reporter_email || \"\").trim() || \"Unknown\"}",
      "8354:                                          reporter_user_id: ev.reporter_user_id || null,",
      "8356:                                          reporter_email: ev.reporter_email || null,",
      "8357:                                          reporter_phone: ev.reporter_phone || null,",
      "8374:                                      {String(ev.reporter_name || \"\").trim() || String(ev.reporter_email || \"\").trim() || \"Unknown\"}",
      "8406:                                          reporter_user_id: r.fixed_event.reporter_user_id || null,",
      "8408:                                          reporter_email: r.fixed_event.reporter_email || null,",
      "8409:                                          reporter_phone: r.fixed_event.reporter_phone || null,",
      "8426:                                      {String(r.fixed_event.reporter_name || \"\").trim() || String(r.fixed_event.reporter_email || \"\").trim() || \"Unknown\"}",
      "8479:                                            reporter_user_id: detail.reporter_user_id || null,",
      "8481:                                            reporter_email: detail.reporter_email || null,",
      "8482:                                            reporter_phone: detail.reporter_phone || null,",
      "8499:                                        {String(detail.reporter_name || \"\").trim() || String(detail.reporter_email || \"\").trim() || \"Unknown\"}",
      "8653:                      {String(item?.row?.reporter_name || \"\").trim() || String(item?.row?.reporter_email || \"\").trim() || \"Unknown\"}",
      "8880:                              {String(r?.reporter_name || \"\").trim() || String(r?.reporter_email || \"\").trim() || \"Unknown\"}",
      "9606:                My Reports",
      "9952:        reporter_user_id: r.reporter_user_id || null,",
      "9954:        reporter_phone: r.reporter_phone || null,",
      "9955:        reporter_email: r.reporter_email || null,",
      "9964:    const actorEmail = normalizeEmail(a?.actor_email || a?.reporter_email || \"\");",
      "9965:    const actorPhone = normalizePhone(a?.actor_phone || a?.reporter_phone || \"\");",
      "9966:    const actorUserId = String(a?.actor_user_id || a?.reporter_user_id || \"\").trim();",
      "10714:        .select(\"id, created_at, lat, lng, report_type, report_quality, note, light_id, report_number, reporter_user_id, reporter_name, reporter_phone, reporter_email\")",
      "10737:      reporter_user_id: r.reporter_user_id || null,",
      "10739:      reporter_phone: r.reporter_phone || null,",
      "10740:      reporter_email: r.reporter_email || null,",
      "10746:      const actorEmail = a.actor_email || a.reporter_email || noteContact.email || null;",
      "10747:      const actorPhone = a.actor_phone || a.reporter_phone || noteContact.phone || null;",
      "10748:      const actorUserId = a.actor_user_id || a.reporter_user_id || null;",
      "10759:        reporter_user_id: a.reporter_user_id || actorUserId,",
      "10761:        reporter_email: a.reporter_email || actorEmail,",
      "10762:        reporter_phone: a.reporter_phone || actorPhone,",
      "10824:        reporter_user_id: r.reporter_user_id || null,",
      "10826:        reporter_email: r.reporter_email || null,",
      "10827:        reporter_phone: r.reporter_phone || null,",
      "12346:      const reportSelectFull = \"id, created_at, lat, lng, report_type, report_quality, note, light_id, report_number, reporter_user_id, reporter_name, reporter_phone, reporter_email\";",
      "12371:            .eq(\"reporter_user_id\", session.user.id)",
      "12429:        \"id, pothole_id, lat, lng, note, report_number, created_at, reporter_user_id, reporter_name, reporter_phone, reporter_email\";",
      "12666:            reporter_user_id: r.reporter_user_id || null,",
      "12668:            reporter_phone: r.reporter_phone || null,",
      "12669:            reporter_email: r.reporter_email || null,",
      "12705:        reporter_user_id: r.reporter_user_id || null,",
      "12707:        reporter_phone: r.reporter_phone || null,",
      "12708:        reporter_email: r.reporter_email || null,",
      "12721:        reporter_user_id: r.reporter_user_id || null,",
      "12723:        reporter_phone: r.reporter_phone || null,",
      "12724:        reporter_email: r.reporter_email || null,",
      "12778:          const actorEmail = a.actor_email || a.reporter_email || noteContact.email || null;",
      "12779:          const actorPhone = a.actor_phone || a.reporter_phone || noteContact.phone || null;",
      "12780:          const actorUserId = a.actor_user_id || a.reporter_user_id || null;",
      "12791:            reporter_user_id: a.reporter_user_id || actorUserId,",
      "12793:            reporter_email: a.reporter_email || actorEmail,",
      "12794:            reporter_phone: a.reporter_phone || actorPhone,",
      "12899:          reporter_user_id: r.reporter_user_id || null,",
      "12901:          reporter_phone: r.reporter_phone || null,",
      "12902:          reporter_email: r.reporter_email || null,",
      "12964:          const actorEmail = a.actor_email || a.reporter_email || noteContact.email || null;",
      "12965:          const actorPhone = a.actor_phone || a.reporter_phone || noteContact.phone || null;",
      "12966:          const actorUserId = a.actor_user_id || a.reporter_user_id || null;",
      "12978:            reporter_user_id: a.reporter_user_id || actorUserId,",
      "12980:            reporter_email: a.reporter_email || actorEmail,",
      "12981:            reporter_phone: a.reporter_phone || actorPhone,",
      "13128:          reporter_user_id: r?.reporter_user_id || null,",
      "13130:          reporter_phone: r?.reporter_phone || null,",
      "13131:          reporter_email: r?.reporter_email || null,",
      "14313:        r.reporter_user_id ? `uid:${r.reporter_user_id}` :",
      "14314:        (normalizeEmail(r.reporter_email) ? `email:${normalizeEmail(r.reporter_email)}` :",
      "14315:         (normalizePhone(r.reporter_phone) ? `phone:${normalizePhone(r.reporter_phone)}` : null));",
      "16226:        reporter_user_id: isAuthed ? session.user.id : null,",
      "16228:        reporter_phone: phone.trim() || null,",
      "16229:        reporter_email: email.trim() || null,",
      "16252:        reporter_user_id: insReport.data?.reporter_user_id || null,",
      "16254:        reporter_phone: insReport.data?.reporter_phone || null,",
      "16255:        reporter_email: insReport.data?.reporter_email || null,",
      "16358:        reporter_user_id: isAuthed ? session.user.id : null,",
      "16360:        reporter_phone: phone.trim() || null,",
      "16361:        reporter_email: email.trim() || null,",
      "16382:        reporter_user_id: data.reporter_user_id || null,",
      "16384:        reporter_phone: data.reporter_phone || null,",
      "16385:        reporter_email: data.reporter_email || null,",
      "16516:      const canReadInsertedRow = Boolean(attempt.reporter_user_id);",
      "16646:      reporter_user_id: isAuthed ? session.user.id : null,",
      "16648:      reporter_phone: normPhone,",
      "16649:      reporter_email: normEmail,",
      "16658:      const canReadInsertedRow = Boolean(attempt.reporter_user_id);",
      "16718:        reporter_user_id: savedWorking.reporter_user_id || null,",
      "16720:        reporter_phone: savedWorking.reporter_phone || normPhone,",
      "16721:        reporter_email: savedWorking.reporter_email || normEmail,",
      "16878:          reporter_user_id: isAuthed ? session.user.id : null,",
      "16880:          reporter_phone: phone.trim() || null,",
      "16881:          reporter_email: email.trim() || null,",
      "16907:      reporter_user_id: data.reporter_user_id || null,",
      "16909:      reporter_phone: data.reporter_phone || null,",
      "16910:      reporter_email: data.reporter_email || null,",
      "16938:    openNotice(\"✅\", \"Report saved\", \"Saved to My Reports for utility follow-up.\", { autoCloseMs: 1200 });",
      "17245:      reporter_user_id: isAuthed ? session.user.id : null,",
      "17247:      reporter_phone: phone.trim() || null,",
      "17248:      reporter_email: email.trim() || null,",
      "17272:      reporter_user_id: data.reporter_user_id || null,",
      "17274:      reporter_phone: data.reporter_phone || null,",
      "17275:      reporter_email: data.reporter_email || null,",
      "17301:    openNotice(\"✅\", \"Reports saved\", `Saved ${okCount} report${okCount === 1 ? \"\" : \"s\"} to My Reports.`);",
      "19694:        modalTitle=\"My Reports\""
    ],
    "marker_ring_logic": [
      "21:  const host = String(hostname || \"\").trim().toLowerCase();",
      "29:  return String(window.location.hostname || \"\").trim().toLowerCase();",
      "61:  String(import.meta.env.VITE_ENABLE_STREETLIGHT_IN_APP_REPORTING || \"\").trim().toLowerCase() === \"true\";",
      "63:  String(import.meta.env.VITE_ENABLE_LEGACY_PLACES_SERVICE || \"\").trim().toLowerCase() === \"true\";",
      "66:  String(import.meta.env.VITE_STREETLIGHT_UTILITY_REPORT_URL || \"\").trim() ||",
      "114:  flickering: \"Dim / Flickering\",",
      "115:  dayburner: \"On during daytime\",",
      "157:const STREET_SIGN_TYPE_VALUES = new Set(STREET_SIGN_TYPE_OPTIONS.map((opt) => String(opt.value || \"\").trim().toLowerCase()));",
      "172:  const d = String(domainKey || \"\").trim().toLowerCase();",
      "197:  const key = String(raw || \"\").trim().toLowerCase();",
      "207:  const key = String(raw || \"\").trim().toLowerCase();",
      "221:  const key = String(raw || \"\").trim().toLowerCase();",
      "226:  const key = String(raw || \"\").trim().toLowerCase();",
      "236:  const text = String(note || \"\");",
      "239:  const raw = String(m?.[1] || \"\").trim().toLowerCase();",
      "330:  if (typeof raw === \"string\") {",
      "339:    const type = String(raw?.type || \"\");",
      "355:    .map((ring) => {",
      "356:      if (!Array.isArray(ring)) return [];",
      "357:      return ring",
      "366:    .filter((ring) => ring.length >= 3);",
      "377:    const type = String(g.type || \"\");",
      "379:      const rings = normalizePolygonRings(g.coordinates);",
      "380:      if (rings.length) out.push(rings);",
      "385:        const rings = normalizePolygonRings(poly);",
      "386:        if (rings.length) out.push(rings);",
      "391:  const type = String(geo.type || \"\");",
      "413:    for (const ring of poly) {",
      "414:      if (!Array.isArray(ring)) continue;",
      "415:      for (const pt of ring) {",
      "443:function isPointInRing(lat, lng, ring) {",
      "444:  if (!Array.isArray(ring) || ring.length < 3) return false;",
      "446:  for (let i = 0, j = ring.length - 1; i < ring.length; j = i++) {",
      "447:    const xi = Number(ring[i]?.lng);",
      "448:    const yi = Number(ring[i]?.lat);",
      "449:    const xj = Number(ring[j]?.lng);",
      "450:    const yj = Number(ring[j]?.lat);",
      "472:function ringSignedArea(ring) {",
      "473:  if (!Array.isArray(ring) || ring.length < 3) return 0;",
      "475:  for (let i = 0; i < ring.length; i += 1) {",
      "476:    const a = ring[i];",
      "477:    const b = ring[(i + 1) % ring.length];",
      "488:function orientRing(ring, direction = \"clockwise\") {",
      "489:  if (!Array.isArray(ring)) return [];",
      "490:  const copy = ring",
      "497:  const area = ringSignedArea(copy);",
      "539:  return String(e || \"\").trim().toLowerCase();",
      "543:  return String(p || \"\").replace(/[^\\d]/g, \"\"); // digits only",
      "547:  return String(v || \"\")",
      "556:  const incidentId = String(target?.lightId || target?.pothole_id || \"\").trim().toLowerCase();",
      "568:  const v = String(lightId || \"\").trim().toLowerCase();",
      "578:  const d = normalizeDomainKey(domain) || String(domain || \"\").trim().toLowerCase();",
      "579:  const id = String(incidentId || \"\").trim();",
      "585:  const v = String(state || \"\").trim().toLowerCase();",
      "600:  const raw = String(rawLightId || \"\").trim();",
      "617:  const persisted = String(row?.report_number || \"\").trim();",
      "645:  if (Number.isFinite(asNum) && asNum > 0) return `${prefix}${String(Math.trunc(asNum)).padStart(7, \"0\")}`;",
      "647:  const s = String(idRaw ?? `${row?.light_id || \"\"}|${row?.pothole_id || \"\"}|${row?.ts || 0}`);",
      "653:  return `${prefix}${String(h % 10000000).padStart(7, \"0\")}`;",
      "657:  const s = String(incidentId || \"\").trim();",
      "664:  const key = (h >>> 0).toString(36).toUpperCase();",
      "670:  const id = String(incidentId || \"\").trim();",
      "671:  const shown = String(displayId || \"\").trim();",
      "678:  const report = String(reportNumber || \"\").trim();",
      "691:  const m = String(name || \"\").trim().match(/\\.([a-zA-Z0-9]{2,8})$/);",
      "696:  const v = String(password || \"\");",
      "706:  return String(t || \"\").trim().toLowerCase();",
      "710:  const raw = String(v || \"\").trim().toLowerCase();",
      "730:  const lid = String(row?.light_id || \"\").trim();",
      "756:  const raw = String(note || \"\").trim();",
      "760:  return String(m[1] || \"\").trim();",
      "764:  const raw = String(note || \"\").trim();",
      "771:  const raw = String(note || \"\").trim();",
      "775:  return String(m[1] || \"\").trim();",
      "779:  const raw = String(note || \"\").trim();",
      "783:  return String(m[1] || \"\").trim();",
      "787:  const raw = String(note || \"\").trim();",
      "791:  const v = String(m[1] || \"\").trim();",
      "798:  const raw = String(note || \"\").trim();",
      "817:  const power = [\"yes\", \"no\"].includes(String(areaPowerOn || \"\").toLowerCase())",
      "818:    ? String(areaPowerOn || \"\").toLowerCase()",
      "820:  const hazard = [\"yes\", \"no\"].includes(String(hazardYesNo || \"\").toLowerCase())",
      "821:    ? String(hazardYesNo || \"\").toLowerCase()",
      "824:  const noteText = String(userNote || \"\").trim();",
      "829:  const raw = String(note || \"\");",
      "833:    powerOn: String(m[1] || \"\").toLowerCase(),",
      "834:    hazardous: String(m[2] || \"\").toLowerCase(),",
      "839:  const v = String(q || \"\").trim().toLowerCase();",
      "862:  const raw = String(note || \"\").trim();",
      "869:    const name = String(parsed.reporter_name || parsed.actor_name || \"\").trim() || null;",
      "890:  const name = String(guestInfo?.name || \"\").trim().toLowerCase();",
      "914:    const lid = String(lightId || \"\").trim();",
      "920:        if (String(r?.pothole_id || \"\").trim() !== pid) continue;",
      "979:// Bearing (direction of travel) from 2 coords, degrees 0-360",
      "980:function bearingBetween(a, b) {",
      "997:function destinationPointMeters(start, distanceMeters, bearingDeg) {",
      "1003:  const bearing = toRad(Number(bearingDeg) || 0);",
      "1013:  const lat2 = Math.asin(sinLat1 * cosAng + cosLat1 * sinAng * Math.cos(bearing));",
      "1015:    Math.sin(bearing) * sinAng * cosLat1,",
      "1053:    localStorage.setItem(COOLDOWNS_KEY, JSON.stringify(cooldowns));",
      "1073:    localStorage.setItem(ABUSE_RATE_KEY, JSON.stringify(value || {}));",
      "1093:    localStorage.setItem(ABUSE_BACKOFF_KEY, JSON.stringify(value || {}));",
      "1101:  const key = String(identityKey || \"\").trim();",
      "1114:  const key = String(identityKey || \"\").trim();",
      "1127:      domain: String(payload?.domain || \"streetlights\"),",
      "1130:      event_kind: String(payload?.event_kind || \"submission_attempt\"),",
      "1134:      reason: payload?.reason ? String(payload.reason) : null,",
      "1136:      created_at: new Date().toISOString(),",
      "1220:    name: String(guestInfo?.name || profile?.full_name || session?.user?.user_metadata?.full_name || \"\").trim() || null,",
      "1233:        idempotency_key: String(idempotencyKey || \"\").trim() || null,",
      "1403:function gmapsDotIcon(color = \"#1976d2\", ringColor = \"white\", glyph = \"💡\", glyphSrc = \"\") {",
      "1405:  const r = ringColor || \"white\";",
      "1406:  const gph = String(glyph || \"💡\");",
      "1407:  const gsrc = String(glyphSrc || \"\").trim();",
      "1490:  const raw = String(src || \"\").trim();",
      "1493:  const borderColor = String(opts?.borderColor || \"#39ff14\");",
      "1962:  getMarkerRingColor,",
      "1975:        icon={gmapsDotIcon(iconColor, getMarkerRingColor?.(ol.id) || \"#fff\", \"💡\", UI_ICON_SRC.streetlight)}",
      "1991:  getMarkerRingColor,",
      "2003:    getMarkerRingColor,",
      "2006:  latestRef.current = { show, lights, bulkMode, bulkSelectedSet, getMarkerColor, getMarkerRingColor };",
      "2080:      ctx.strokeStyle = state.getMarkerRingColor?.(ol.id) || \"#fff\";",
      "2190:  }, [drawOverlayCanvas, show, lights, bulkMode, bulkSelectedSet, getMarkerColor, getMarkerRingColor]);",
      "2562:    : Boolean(String(streetSignIssue || \"\").trim());",
      "2950:  const emailLooksValid = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(String(email || \"\").trim());",
      "3020:    { ring = \"#fff\", glyph = \"💡\", glyphSrc = \"\", glyphColor = \"#111\", showGlyph = true } = {}",
      "3028:        border: `${MAP_MARKER_STROKE}px solid ${ring}`,",
      "3052:        { swatch: markerSwatch(\"#111\", { ring: \"#2ecc71\", showGlyph: false }), label: \"Saved to My Reports (green outline)\" },",
      "3053:        { swatch: markerSwatch(\"#111\", { ring: \"#1976d2\", showGlyph: false }), label: \"Reported to utility (blue outline)\" },",
      "3311:  const hasSignupLen = String(signupPassword || \"\").length >= 8;",
      "3312:  const hasSignupUpper = /[A-Z]/.test(String(signupPassword || \"\"));",
      "3313:  const hasSignupLower = /[a-z]/.test(String(signupPassword || \"\"));",
      "3314:  const hasSignupNumber = /[0-9]/.test(String(signupPassword || \"\"));",
      "3315:  const hasSignupSpecial = /[^A-Za-z0-9]/.test(String(signupPassword || \"\"));",
      "3469:          {!!String(loginError || \"\").trim() && (",
      "3936:        .map((it) => String(it?.actor_user_id || \"\").trim())",
      "3953:        const uid = String(row?.user_id || \"\").trim();",
      "3956:          name: String(row?.full_name || \"\").trim() || null,",
      "3957:          phone: String(row?.phone || \"\").trim() || null,",
      "3958:          email: String(row?.email || \"\").trim() || null,",
      "3993:      {!!String(sharedLocation || sharedAddress || sharedLandmark).trim() && (",
      "3995:          {!!String(sharedLocation || \"\").trim() && (",
      "4020:          {!!String(sharedAddress || \"\").trim() && (",
      "4045:          {!!String(sharedLandmark || \"\").trim() && (",
      "4051:      {!!String(currentState || \"\").trim() && (",
      "4054:          {!!String(lastChangedAt || \"\").trim() && (",
      "4080:            const actorUserId = String(it?.actor_user_id || \"\").trim();",
      "4083:              String(it?.actor_name || \"\").trim()",
      "4084:              || String(actorProfile?.name || \"\").trim()",
      "4085:              || String(it?.actor_email || \"\").trim()",
      "4086:              || String(actorProfile?.email || \"\").trim()",
      "4087:              || String(it?.actor_phone || \"\").trim()",
      "4088:              || String(actorProfile?.phone || \"\").trim()",
      "4091:              String(it?.actor_email || \"\").trim()",
      "4092:              || String(actorProfile?.email || \"\").trim()",
      "4095:              String(it?.actor_phone || \"\").trim()",
      "4096:              || String(actorProfile?.phone || \"\").trim()",
      "4102:                [\"downed_pole\", \"pole_down\", \"downed-pole\"].includes(String(it.type || \"\").toLowerCase());",
      "4171:                      {String(it?.reporter_name || \"\").trim() || String(it?.reporter_email || \"\").trim() || \"Unknown\"}",
      "4290:  const normalizedQuery = String(searchQuery || \"\").trim().toLowerCase();",
      "4295:      const displayId = String(g?.displayId || displayLightId(g?.lightId, slIdByUuid) || \"\").toLowerCase();",
      "4301:          const type = String(REPORT_TYPES?.[r?.type] || r?.type || \"\").toLowerCase();",
      "4302:          const note = String(stripSystemMetadataFromNote(r?.note || \"\") || r?.note || \"\").toLowerCase();",
      "4303:          const email = String(r?.reporter_email || \"\").toLowerCase();",
      "4304:          const name = String(r?.reporter_name || \"\").toLowerCase();",
      "4418:            if (e.key === \"Enter\") setSearchQuery(String(searchDraft || \"\").trim());",
      "4433:          onClick={() => setSearchQuery(String(searchDraft || \"\").trim())}",
      "4653:                                {!!String(stripSystemMetadataFromNote(r.note || \"\") || \"\").trim() && (",
      "4695:      const n = Date.parse(String(v || \"\"));",
      "4702:    const closed = String(row?.resolved_at || row?.closed_at || row?.cleared_at || \"\").trim();",
      "4711:    return String(row?.domain || row?.report_domain || row?.incident_domain || \"unknown\").trim() || \"unknown\";",
      "4715:    return String(",
      "4726:    return String(",
      "4824:                <tr key={`${String(row?.id || \"flag\")}-${idx}`}>",
      "4858:    return d.toLocaleString([], {",
      "4874:  const m = String(d.getMonth() + 1).padStart(2, \"0\");",
      "4875:  const day = String(d.getDate()).padStart(2, \"0\");",
      "4880:  const s = String(value || \"\").trim();",
      "4998:    if (!String(from || \"\").trim() && !String(to || \"\").trim()) return \"All\";",
      "5010:    const from = String(exportFromDate || \"\").trim();",
      "5011:    const to = String(exportToDate || \"\").trim();",
      "5024:    const rawFrom = String(draftFromDate || \"\").trim();",
      "5025:    const rawTo = String(draftToDate || \"\").trim();",
      "5064:    return d.toLocaleDateString(undefined, { month: \"short\", year: \"numeric\" });",
      "5082:    const day = String(iso || \"\").trim();",
      "5084:    const from = String(draftFromDate || \"\").trim();",
      "5085:    const to = String(draftToDate || \"\").trim();",
      "5098:  const draftRangeFrom = String(draftFromDate || \"\").trim();",
      "5099:  const draftRangeTo = String(draftToDate || draftFromDate || \"\").trim();",
      "5101:    const day = String(iso || \"\").trim();",
      "5131:    topRecurring: [],",
      "5141:    const text = `${String(err?.message || \"\")} ${String(err?.details || \"\")} ${String(err?.hint || \"\")}`.toLowerCase();",
      "5150:    () => new Set((officialLights || []).map((l) => String(l?.id || \"\").trim()).filter(Boolean)),",
      "5155:    const raw = String(rawAddress || \"\").trim();",
      "5159:    const parts = raw.split(\",\").map((s) => String(s || \"\").trim()).filter(Boolean);",
      "5164:    const houseNumber = firstMatch ? String(firstMatch[1]).trim() : \"\";",
      "5165:    const street = firstMatch ? String(firstMatch[2]).trim() : first;",
      "5174:      state: stateZipMatch ? String(stateZipMatch[1]).trim() : \"\",",
      "5175:      zip: stateZipMatch ? String(stateZipMatch[2]).trim() : \"\",",
      "5178:  const isMyReportsModal = String(modalTitle || \"\").trim().toLowerCase() === \"my reports\";",
      "5180:  const utilityReportUserId = String(session?.user?.id || \"\").trim();",
      "5187:  const hasSearchText = Boolean(String(searchDraft || \"\").trim() || String(searchQuery || \"\").trim());",
      "5198:    const text = String(value || \"\").trim();",
      "5224:    const nearestAddressRaw = String(util?.nearestAddress || util?.nearest_address || \"\").trim();",
      "5226:    const onStreet = String(util?.nearestStreet || util?.nearest_street || \"\").trim();",
      "5227:    const intersectionRaw = String(util?.nearestIntersection || util?.nearest_intersection || \"\").trim();",
      "5228:    const nearestCrossStreetRaw = String(util?.nearestCrossStreet || util?.nearest_cross_street || \"\").trim();",
      "5229:    const normalizeRoad = (v) => String(v || \"\").toLowerCase().replace(/[^a-z0-9]/g, \"\");",
      "5240:        .map((x) => String(x || \"\").trim())",
      "5260:      { label: \"Nearest Landmark\", value: String(util?.nearestLandmark || util?.nearest_landmark || \"\").trim() || \"Unavailable\" },",
      "5306:    const targetIncidentId = String(focusIncidentId || \"\").trim();",
      "5313:    const q = String(initialSearchQuery || \"\").trim();",
      "5341:        const id = String(row?.incident_id || \"\").trim();",
      "5353:    const id = String(incidentId || \"\").trim();",
      "5369:            reported_at: new Date().toISOString(),",
      "5400:    const key = String(incidentId || \"\").trim();",
      "5406:    const byId = (officialLights || []).find((l) => String(l?.id || \"\").trim() === key);",
      "5409:      : (officialLights || []).find((l) => String(l?.sl_id || \"\").trim().toLowerCase() === key.toLowerCase());",
      "5414:      nearestAddress: String(match?.nearest_address || \"\").trim(),",
      "5415:      nearestCrossStreet: String(match?.nearest_cross_street || \"\").trim(),",
      "5416:      nearestLandmark: String(match?.nearest_landmark || \"\").trim(),",
      "5451:      .map((id) => String(id || \"\").trim())",
      "5461:    const id = String(incidentId || \"\").trim();",
      "5468:      const kind = String(a?.action || \"\").toLowerCase();",
      "5484:      return { state: \"reported\", fixedAtIso: \"\", lastChangedAtIso: new Date(lastReportTs).toISOString() };",
      "5487:      return { state: \"reopened\", fixedAtIso: \"\", lastChangedAtIso: new Date(lastReopenTs).toISOString() };",
      "5490:      return { state: \"fixed\", fixedAtIso: new Date(lastFixTs).toISOString(), lastChangedAtIso: new Date(lastFixTs).toISOString() };",
      "5493:      return { state: \"reported\", fixedAtIso: \"\", lastChangedAtIso: new Date(lastReportTs).toISOString() };",
      "5539:      const incidentId = String(g?.incidentId || g?.lightId || \"\").trim();",
      "5545:    const raw = String(searchQuery || \"\").trim();",
      "5557:    const q = String(searchQuery || \"\").trim().toLowerCase();",
      "5560:      const s = String(exportFromDate || \"\").trim();",
      "5565:      const s = String(exportToDate || \"\").trim();",
      "5590:        String(g.location_label || \"\").trim() ||",
      "5599:        const name = String(r?.reporter_name || \"\").toLowerCase();",
      "5600:        const email = String(r?.reporter_email || \"\").toLowerCase();",
      "5602:        const lightId = String(g?.lightId || \"\").toLowerCase();",
      "5603:        const incidentIdRaw = String(r?.incident_id || g?.incidentId || g?.lightId || \"\").trim();",
      "5604:        const groupDisplayIdRaw = String(g?.displayId || \"\").trim();",
      "5629:        const displayIdNorm = String(displayId || \"\").toLowerCase();",
      "5680:    return String(v || \"\")",
      "5688:    const s = String(state || \"\").trim().toLowerCase();",
      "5735:        if (from) q = q.gte(\"submitted_at\", from.toISOString());",
      "5736:        if (toExclusive) q = q.lt(\"submitted_at\", toExclusive.toISOString());",
      "5738:        const sq = String(searchQuery || \"\").trim();",
      "5759:          setServerDetailError(String(e?.message || e || \"Failed to load export dataset\"));",
      "5791:            topRecurring: [],",
      "5797:        const [stateCountsRes, closeStatsRes, topRecurringRes] = await Promise.all([",
      "5808:            .from(\"metrics_top_recurring_incidents_v1\")",
      "5818:        if (topRecurringRes.error) throw topRecurringRes.error;",
      "5824:            topRecurring: Array.isArray(topRecurringRes.data) ? topRecurringRes.data : [],",
      "5832:            topRecurring: [],",
      "5857:    if (String(searchQuery || \"\").trim()) {",
      "5863:          ? String(item.lightId || \"\")",
      "5864:          : String(item.row?.incident_id || item.lightId || \"\");",
      "5867:            const fromRowPid = String(item?.row?.pothole_id || \"\").trim();",
      "5875:          report_id: String(item?.row?.id || \"\"),",
      "5877:          report_type: String(item?.row?.type || item?.row?.report_type || \"\"),",
      "5880:          submitted_at: ts ? new Date(ts).toISOString() : \"\",",
      "5881:          fixed_at: snapshot?.state === \"fixed\" ? String(snapshot?.last_changed_at || \"\") : \"\",",
      "5883:          current_state: String(snapshot?.state || \"\"),",
      "5884:          reporter_name: String(item?.row?.reporter_name || \"\"),",
      "5885:          reporter_email: String(item?.row?.reporter_email || \"\"),",
      "5886:          reporter_phone: String(item?.row?.reporter_phone || \"\"),",
      "5887:          notes: String(stripSystemMetadataFromNote(item?.row?.note || \"\") || \"\"),",
      "5898:          const pid = String(r?.pothole_id || \"\").trim();",
      "5902:          incidentId = String(r?.light_id || \"\").trim();",
      "5915:        if (inViewIncidentIdSet && !inViewIncidentIdSet.has(String(incidentId || \"\").trim())) continue;",
      "5922:            report_id: String(r?.id || \"\"),",
      "5924:            report_type: String(r?.type || r?.report_type || \"\"),",
      "5927:            submitted_at: ts ? new Date(ts).toISOString() : \"\",",
      "5931:            reporter_name: String(r?.reporter_name || \"\"),",
      "5932:            reporter_email: String(r?.reporter_email || \"\"),",
      "5933:            reporter_phone: String(r?.reporter_phone || \"\"),",
      "5934:            notes: String(stripSystemMetadataFromNote(r?.note || \"\") || \"\"),",
      "5940:        const incidentId = String(g?.incidentId || g?.lightId || \"\");",
      "5947:            report_id: String(r?.id || \"\"),",
      "5949:            report_type: String(r?.type || r?.report_type || \"\"),",
      "5952:            submitted_at: ts ? new Date(ts).toISOString() : \"\",",
      "5953:            fixed_at: timelineState.fixedAtIso || (snapshot?.state === \"fixed\" ? String(snapshot?.last_changed_at || \"\") : \"\"),",
      "5955:            current_state: String(timelineState.state || snapshot?.state || \"\"),",
      "5956:            reporter_name: String(r?.reporter_name || \"\"),",
      "5957:            reporter_email: String(r?.reporter_email || \"\"),",
      "5958:            reporter_phone: String(r?.reporter_phone || \"\"),",
      "5959:            notes: String(stripSystemMetadataFromNote(r?.note || \"\") || \"\"),",
      "5964:    return detail.sort((a, b) => String(b.submitted_at).localeCompare(String(a.submitted_at)));",
      "5984:      (potholes || []).map((p) => [String(p?.id || \"\").trim(), p]).filter(([id]) => !!id)",
      "5987:      const d = String(domain || \"\").trim().toLowerCase();",
      "5988:      const id = String(incidentId || \"\").trim();",
      "6006:        const domain = String(r?.domain ?? activeDomain);",
      "6007:        const incidentId = String(r?.incident_id ?? \"\");",
      "6011:          .filter((a) => String(a?.action || \"\").toLowerCase() === \"fix\")",
      "6013:        const submittedAt = String(r?.submitted_at ?? \"\");",
      "6017:            ? new Date(Number(latestFixAction.ts)).toISOString()",
      "6019:        const fixedAt = String(",
      "6025:        const inferredState = String(",
      "6032:          report_id: String(r?.report_id ?? \"\"),",
      "6033:          report_number: String(r?.report_number ?? \"\"),",
      "6034:          report_type: String(r?.report_type ?? \"\"),",
      "6045:          reporter_name: String(r?.reporter_name ?? \"\"),",
      "6046:          reporter_email: String(r?.reporter_email ?? \"\"),",
      "6047:          reporter_phone: String(r?.reporter_phone ?? \"\"),",
      "6048:          notes: String(r?.notes ?? \"\"),",
      "6052:        const domain = String(r?.domain ?? activeDomain);",
      "6053:        const incidentId = String(r?.incident_id ?? \"\");",
      "6062:      return baseRows.filter((r) => officialIdSetForExport.has(String(r?.incident_id || \"\").trim()));",
      "6065:      return baseRows.filter((r) => officialSignIdSetForExport.has(String(r?.incident_id || \"\").trim()));",
      "6105:      if (String(r.submitted_at) < String(prev.first_reported_at)) prev.first_reported_at = r.submitted_at;",
      "6106:      if (String(r.submitted_at) > String(prev.latest_reported_at)) prev.latest_reported_at = r.submitted_at;",
      "6111:      const firstMs = Date.parse(String(r.first_reported_at || \"\"));",
      "6112:      const fixedMs = Date.parse(String(r.fixed_at || \"\"));",
      "6120:      String(b.latest_reported_at).localeCompare(String(a.latest_reported_at))",
      "6127:      const key = String(g?.incidentId || g?.lightId || \"\").trim();",
      "6137:      (potholes || []).map((p) => [String(p?.id || \"\").trim(), p]).filter(([id]) => !!id)",
      "6140:      const incidentId = String(r?.incident_id || \"\").trim();",
      "6154:          incident_display_id: String(g?.lightId || \"\").trim(),",
      "6155:          incident_public_id: String(r?.incident_public_id || \"\").trim(),",
      "6158:          current_state: String(r?.current_state || \"\"),",
      "6159:          fixed_at: String(r?.fixed_at || \"\"),",
      "6169:        report_id: String(r?.report_id || \"\"),",
      "6170:        report_number: String(r?.report_number || \"\"),",
      "6171:        report_type: String(r?.report_type || \"\"),",
      "6172:        submitted_at: String(r?.submitted_at || \"\"),",
      "6173:        reporter_name: String(r?.reporter_name || \"\"),",
      "6174:        reporter_email: String(r?.reporter_email || \"\"),",
      "6175:        reporter_phone: String(r?.reporter_phone || \"\"),",
      "6176:        notes: String(r?.notes || \"\"),",
      "6178:      if (!item.latest_submitted_at || String(r?.submitted_at || \"\") > String(item.latest_submitted_at)) {",
      "6179:        item.latest_submitted_at = String(r?.submitted_at || \"\");",
      "6181:      if (!item.incident_public_id && String(r?.incident_public_id || \"\").trim()) {",
      "6182:        item.incident_public_id = String(r.incident_public_id || \"\").trim();",
      "6184:      if (!item.fixed_at || String(r?.fixed_at || \"\") > String(item.fixed_at)) {",
      "6185:        item.fixed_at = String(r?.fixed_at || \"\");",
      "6192:        const pid = String(row?.incident_id || \"\").replace(/^pothole:/i, \"\").trim();",
      "6198:      row.rows.sort((a, b) => String(b.submitted_at).localeCompare(String(a.submitted_at)));",
      "6199:      row.primary_report_number = String(row.rows?.[0]?.report_number || \"\").trim();",
      "6200:      const shownDisplay = String(row.incident_display_id || \"\").trim();",
      "6211:        const exportedWd = String(row?.incident_public_id || \"\").trim();",
      "6226:        .filter((a) => String(a?.action || \"\").toLowerCase() === \"fix\")",
      "6229:        .filter((a) => String(a?.action || \"\").toLowerCase() === \"reopen\")",
      "6233:          note: String(a?.note || \"\").trim(),",
      "6235:          reporter_name: String(a?.actor_name || a?.reporter_name || \"\").trim() || null,",
      "6236:          reporter_email: String(a?.actor_email || a?.reporter_email || \"\").trim() || null,",
      "6237:          reporter_phone: String(a?.actor_phone || a?.reporter_phone || \"\").trim() || null,",
      "6242:          String(Math.floor(Number(ev.ts || 0) / 1000)),",
      "6243:          String(ev.note || \"\"),",
      "6244:          String(ev.reporter_user_id || \"\"),",
      "6245:          String(ev.reporter_name || \"\"),",
      "6256:          String(fixedByUserId) === String(session.user.id)",
      "6261:              String(profile?.full_name || \"\").trim() ||",
      "6262:              String(session?.user?.user_metadata?.full_name || \"\").trim() ||",
      "6263:              String(session?.user?.email || \"\").split(\"@\")[0]",
      "6277:        const fixTs = Number(fixAction?.ts || Date.parse(String(row.fixed_at || \"\")) || 0);",
      "6280:          note: String(fixAction?.note || \"\").trim(),",
      "6282:          reporter_name: String(fixAction?.actor_name || fixAction?.reporter_name || \"\").trim() || fallbackName || null,",
      "6283:          reporter_email: String(fixAction?.actor_email || fixAction?.reporter_email || \"\").trim() || fallbackEmail || null,",
      "6284:          reporter_phone: String(fixAction?.actor_phone || fixAction?.reporter_phone || \"\").trim() || fallbackPhone || null,",
      "6289:      row.latest_activity_at = String(",
      "6291:          ? new Date(Number(row.fixed_event.ts)).toISOString()",
      "6295:        const reportTs = Date.parse(String(row.latest_submitted_at || \"\")) || 0;",
      "6298:          row.latest_activity_at = String(row.latest_submitted_at || \"\");",
      "6313:      ? cityFilteredRows.filter((row) => inViewIncidentIdSet.has(String(row?.incident_id || \"\").trim()))",
      "6317:    const key = String(tableSort?.key || \"submitted_at\");",
      "6326:        const ta = Date.parse(String(a.latest_activity_at || a.latest_submitted_at || \"\")) || 0;",
      "6327:        const tb = Date.parse(String(b.latest_activity_at || b.latest_submitted_at || \"\")) || 0;",
      "6331:        const la = String(a?.incident_label || a?.incident_id || \"\").toLowerCase();",
      "6332:        const lb = String(b?.incident_label || b?.incident_id || \"\").toLowerCase();",
      "6337:      const sa = String(a?.[key] || \"\").toLowerCase();",
      "6338:      const sb = String(b?.[key] || \"\").toLowerCase();",
      "6377:    const key = String(tableSort?.key || \"\");",
      "6378:    const dir = String(tableSort?.dir || \"desc\");",
      "6387:    const p = String(preset || \"\").trim().toLowerCase();",
      "6400:    const id = String(incidentId || \"\").trim();",
      "6409:    const incidentId = String(row?.incident_id || \"\").trim();",
      "6413:      const stateText = String(row?.current_state || \"\").trim().toLowerCase();",
      "6463:      const incidentId = String(r?.incident_id || \"\").trim();",
      "6465:      const ts = Date.parse(String(r?.submitted_at || \"\")) || 0;",
      "6470:          state: String(r?.current_state || \"\").trim().toLowerCase(),",
      "6484:      const incidentId = String(r?.incident_id || \"\").trim();",
      "6486:      const submittedMs = Date.parse(String(r?.submitted_at || \"\")) || 0;",
      "6487:      const fixedMs = Date.parse(String(r?.fixed_at || \"\")) || 0;",
      "6502:    const topRecurring = Array.isArray(metricsViewRows.topRecurring) && metricsViewRows.topRecurring.length",
      "6503:      ? metricsViewRows.topRecurring.map((r) => ({",
      "6517:      topRecurring,",
      "6523:      const s = String(v ?? \"\");",
      "6531:        metaLines.push(`# ${String(k)}: ${String(v ?? \"\")}`);",
      "6545:      generated_at_utc: new Date().toISOString(),",
      "6546:      window_start_utc: from ? from.toISOString() : \"\",",
      "6547:      window_end_utc: toExclusive ? toExclusive.toISOString() : \"\",",
      "6584:        const normalizedStatus = String(statusFilter || \"\").toLowerCase();",
      "6602:            search: String(searchQuery || \"\").trim(),",
      "6927:                      if (e.key === \"Enter\") setSearchQuery(String(searchDraft || \"\").trim());",
      "6970:                  onClick={() => setSearchQuery(String(searchDraft || \"\").trim())}",
      "6991:                      onChange={(e) => setStatusFilter(String(e.target.value || \"open\"))}",
      "7055:                onChange={(e) => setStatusFilter(String(e.target.value || \"open\"))}",
      "7079:                    if (e.key === \"Enter\") setSearchQuery(String(searchDraft || \"\").trim());",
      "7122:                onClick={() => setSearchQuery(String(searchDraft || \"\").trim())}",
      "7200:                  if (e.key === \"Enter\") setSearchQuery(String(searchDraft || \"\").trim());",
      "7243:              onClick={() => setSearchQuery(String(searchDraft || \"\").trim())}",
      "7265:                  onChange={(e) => setStatusFilter(String(e.target.value || \"open\"))}",
      "7944:                              {String(ev.reporter_name || \"\").trim() || String(ev.reporter_email || \"\").trim() || \"Unknown\"}",
      "7947:                          {!!String(ev.note || \"\").trim() && (",
      "7996:                              {String(r.fixed_event.reporter_name || \"\").trim() || String(r.fixed_event.reporter_email || \"\").trim() || \"Unknown\"}",
      "7999:                          {!!String(r.fixed_event.note || \"\").trim() && (",
      "8029:                                  <b>What are you seeing:</b> {REPORT_TYPES?.[String(detail.report_type || \"\").trim()] || \"Streetlight issue\"}",
      "8052:                                      ts: Date.parse(String(detail.submitted_at || \"\")) || 0,",
      "8067:                                  {String(detail.reporter_name || \"\").trim() || String(detail.reporter_email || \"\").trim() || \"Unknown\"}",
      "8071:                            {!!String(noteText || \"\").trim() && (",
      "8374:                                      {String(ev.reporter_name || \"\").trim() || String(ev.reporter_email || \"\").trim() || \"Unknown\"}",
      "8377:                                  {!!String(ev.note || \"\").trim() && (",
      "8426:                                      {String(r.fixed_event.reporter_name || \"\").trim() || String(r.fixed_event.reporter_email || \"\").trim() || \"Unknown\"}",
      "8429:                                  {!!String(r.fixed_event.note || \"\").trim() && (",
      "8461:                                        <b>What are you seeing:</b> {REPORT_TYPES?.[String(detail.report_type || \"\").trim()] || \"Streetlight issue\"}",
      "8484:                                            ts: Date.parse(String(detail.submitted_at || \"\")) || 0,",
      "8499:                                        {String(detail.reporter_name || \"\").trim() || String(detail.reporter_email || \"\").trim() || \"Unknown\"}",
      "8503:                                  {!!String(noteText || \"\").trim() && (",
      "8547:        ) : (String(searchQuery || \"\").trim() ? (",
      "8630:                {!!String(item.row?.note || \"\").trim() && (",
      "8653:                      {String(item?.row?.reporter_name || \"\").trim() || String(item?.row?.reporter_email || \"\").trim() || \"Unknown\"}",
      "8694:              String(g.location_label || \"\").trim() ||",
      "8699:            const groupIncidentId = String(",
      "8708:            const lifecycleState = String(",
      "8880:                              {String(r?.reporter_name || \"\").trim() || String(r?.reporter_email || \"\").trim() || \"Unknown\"}",
      "9138:            if (e.key === \"Enter\" && !saving && String(password || \"\").trim()) onConfirm();",
      "9170:          disabled={saving || !String(password || \"\").trim()}",
      "9196:  const hasLen = String(password || \"\").length >= 8;",
      "9197:  const hasUpper = /[A-Z]/.test(String(password || \"\"));",
      "9198:  const hasLower = /[a-z]/.test(String(password || \"\"));",
      "9199:  const hasNumber = /[0-9]/.test(String(password || \"\"));",
      "9200:  const hasSpecial = /[^A-Za-z0-9]/.test(String(password || \"\"));",
      "9203:  const hasCurrentPassword = String(currentPassword || \"\").trim().length > 0;",
      "9369:  const hasLen = String(password || \"\").length >= 8;",
      "9370:  const hasUpper = /[A-Z]/.test(String(password || \"\"));",
      "9371:  const hasLower = /[a-z]/.test(String(password || \"\"));",
      "9372:  const hasNumber = /[0-9]/.test(String(password || \"\"));",
      "9373:  const hasSpecial = /[^A-Za-z0-9]/.test(String(password || \"\"));",
      "9751:    return new Date(ts).toLocaleString();",
      "9786:  const s = String(incidentId || \"\").trim();",
      "9791:    const lat5 = String(Math.abs(Number(m[1])).toFixed(5).split(\".\")[1] || \"00000\").slice(0, 5).padEnd(5, \"0\");",
      "9792:    const lng5 = String(Math.abs(Number(m[2])).toFixed(5).split(\".\")[1] || \"00000\").slice(0, 5).padEnd(5, \"0\");",
      "9801:  return `WD${String(h >>> 0).padStart(10, \"0\").slice(-10)}`;",
      "9808:    const id = String(raw || \"\").trim();",
      "9825:    const id = String(r?.light_id || \"\").trim();",
      "9961:    const action = String(a.action || \"\").toLowerCase();",
      "9963:    const actorName = String(a?.actor_name || a?.reporter_name || \"\").trim();",
      "9966:    const actorUserId = String(a?.actor_user_id || a?.reporter_user_id || \"\").trim();",
      "10037:    nearest_address: String(row?.nearest_address || \"\").trim() || \"\",",
      "10038:    nearest_cross_street: String(row?.nearest_cross_street || \"\").trim() || \"\",",
      "10039:    nearest_landmark: String(row?.nearest_landmark || \"\").trim() || \"\",",
      "10051:    id: String(row.id).trim(),",
      "10052:    sign_type: String(row.sign_type || \"other\").trim().toLowerCase() || \"other\",",
      "10120:      const host = String(window.location.host || \"\").trim();",
      "10169:    // Keep the blue dot responsive, but avoid re-rendering on every noisy GPS tick.",
      "10353:    const rawTitle = String(title ?? \"\").trim();",
      "10354:    const rawMessage = String(message ?? \"\").trim();",
      "10355:    const rawIcon = String(icon ?? \"\");",
      "10394:    const raw = String(value || \"\").trim();",
      "10410:    const msg = String(reason || \"\").trim();",
      "10427:    const msg = String(text || \"\").trim();",
      "10445:    // Realtime channel status strings",
      "10446:    if (typeof errOrStatus === \"string\") {",
      "10452:    const rawCode = String(errOrStatus?.code || \"\").toUpperCase();",
      "10453:    const msg = String(errOrStatus?.message || \"\").toLowerCase();",
      "10454:    const details = String(errOrStatus?.details || \"\").toLowerCase();",
      "10455:    const hint = String(errOrStatus?.hint || \"\").toLowerCase();",
      "10486:    const rawCode = String(err?.code || \"\").toUpperCase();",
      "10487:    const combined = `${String(err?.message || \"\").toLowerCase()} ${String(err?.details || \"\").toLowerCase()} ${String(err?.hint || \"\").toLowerCase()}`;",
      "10580:    const domainKey = String(opts?.domainKey || adminReportDomain || \"potholes\").trim() || \"potholes\";",
      "10581:    const focusIncidentId = String(opts?.focusIncidentId || \"\").trim();",
      "10582:    const focusQuery = String(opts?.focusQuery || \"\").trim();",
      "10615:    const id = String(incidentId || \"\").trim();",
      "10626:    const normalized = String(mode || \"\").trim().toLowerCase();",
      "10640:      domainKey: String(opts?.domainKey || \"streetlights\").trim() || \"streetlights\",",
      "10641:      sharedLocation: String(opts?.sharedLocation || \"\").trim(),",
      "10642:      sharedAddress: String(opts?.sharedAddress || \"\").trim(),",
      "10643:      sharedLandmark: String(opts?.sharedLandmark || \"\").trim(),",
      "10644:      currentState: String(opts?.currentState || \"\").trim(),",
      "10645:      lastChangedAt: String(opts?.lastChangedAt || \"\").trim(),",
      "10654:    const id = String(incidentId || \"\").trim();",
      "10659:      const normalized = normalizeDomainKey(d) || String(d || \"\").trim().toLowerCase();",
      "10687:      if (String(key || \"\").endsWith(`:${id}`)) return value || null;",
      "10694:    const incidentId = String(incidentIdRaw || \"\").trim();",
      "10750:      const actorNameFallback = actorEmail ? String(actorEmail).split(\"@\")[0] : \"\";",
      "10806:    const pid = String(marker?.pothole_id || \"\").trim();",
      "10813:        : (String(marker?.id || \"\").trim() || \"PH0000000000\");",
      "10815:      .filter((r) => String(r?.pothole_id || \"\").trim() === pid)",
      "10831:        const t = String(a?.action || \"\").toLowerCase();",
      "10846:    const markerGeoAddress = String(marker?._geoNearestAddress || \"\").trim();",
      "10847:    const markerGeoLandmark = String(marker?._geoNearestLandmark || \"\").trim();",
      "10885:    const msg = String(error?.message || \"\").toLowerCase();",
      "10886:    const details = String(error?.details || \"\").toLowerCase();",
      "10887:    const hint = String(error?.hint || \"\").toLowerCase();",
      "10911:    const pid = String(marker?.pothole_id || \"\").trim();",
      "10913:    const noteClean = String(noteText || \"\").trim();",
      "10936:          String(profile?.full_name || \"\").trim() ||",
      "10937:          String(session?.user?.user_metadata?.full_name || \"\").trim() ||",
      "10938:          String(session?.user?.email || \"\").split(\"@\")[0] ||",
      "10949:      const nextIso = new Date(ts).toISOString();",
      "11073:    const key = String(domainKey || \"\").trim();",
      "11078:    const configured = String(tenantVisibilityByDomain?.[key] || \"\").trim().toLowerCase();",
      "11164:  const [pendingGuestAction, setPendingGuestAction] = useState(null); // { kind: \"report\" | \"working\" | \"bulk\", lightId?: string }",
      "11303:    const nextKey = String(domainKey || \"\").trim();",
      "11310:      setPendingDomainSwitchTarget({ key: nextKey, label: String(domainLabel || nextKey) });",
      "11316:    showToolHint(`Domain: ${String(domainLabel || nextKey)}`, 1000, 4);",
      "11333:    showToolHint(`Domain: ${String(pendingDomainSwitchTarget.label || pendingDomainSwitchTarget.key)}`, 1000, 4);",
      "11346:    showToolHint(`Domain: ${String(pendingDomainSwitchTarget.label || pendingDomainSwitchTarget.key)}`, 1000, 4);",
      "11389:    const tempId = `tmp_${Date.now()}_${Math.random().toString(16).slice(2)}`;",
      "11408:    const nextType = String(signType || \"\").trim().toLowerCase();",
      "11414:    const tempId = `tmp_${Date.now()}_${Math.random().toString(16).slice(2)}`;",
      "11461:    const nextType = String(signType || \"other\").trim().toLowerCase() || \"other\";",
      "11599:        const errCode = String(error?.code || \"\").trim();",
      "11600:        const errMsg = String(error?.message || \"\").trim();",
      "11616:        const key = String(row?.domain || \"\").trim();",
      "11618:        next[key] = String(row?.visibility || \"\").trim().toLowerCase();",
      "11649:        const errCode = String(error?.code || \"\").trim();",
      "11650:        const errMsg = String(error?.message || \"\").trim();",
      "11664:      const nextBorderColorRaw = String(data?.boundary_border_color || \"\").trim();",
      "11777:    setModerationFlagsError(String(lastErr?.message || lastErr || \"Unable to load moderation flags\"));",
      "11958:          legal_accepted_at: new Date().toISOString(),",
      "12119:    const p1 = String(changePasswordValue || \"\");",
      "12120:    const p2 = String(changePasswordValue2 || \"\");",
      "12121:    const current = String(changePasswordCurrentValue || \"\");",
      "12136:    const email = String(session?.user?.email || profile?.email || \"\").trim().toLowerCase();",
      "12168:    const p1 = String(recoveryPasswordValue || \"\");",
      "12169:    const p2 = String(recoveryPasswordValue2 || \"\");",
      "12212:    const email = String(session?.user?.email || profile?.email || \"\").trim().toLowerCase();",
      "12213:    const password = String(reauthPassword || \"\");",
      "12357:            const msg = String(first?.error?.message || \"\").toLowerCase();",
      "12480:        const msg = String(e?.message || \"\").toLowerCase();",
      "12494:          const configuredBoundaryKey = String(tenantBoundary.data.boundary_config_key || \"\").trim();",
      "12524:            const bucket = String(newest?.storage_bucket || \"tenant-files\").trim() || \"tenant-files\";",
      "12525:            const path = String(newest?.storage_path || \"\").trim();",
      "12528:              const signedUrl = String(signed?.data?.signedUrl || \"\").trim();",
      "12579:            state: String(row?.state || \"\").trim(),",
      "12587:            const keyStr = String(key || \"\");",
      "12592:            const state = String(snap?.state || \"\").trim().toLowerCase();",
      "12594:            const ts = Date.parse(String(snap?.last_changed_at || \"\")) || 0;",
      "12606:          const key = String(row?.incident_id || \"\").trim();",
      "12609:            wd_id: String(row?.wd_id || \"\").trim(),",
      "12610:            issue_type: String(row?.issue_type || \"\").trim().toLowerCase(),",
      "12613:            nearest_address: String(row?.nearest_address || \"\").trim(),",
      "12614:            nearest_cross_street: String(row?.nearest_cross_street || \"\").trim(),",
      "12615:            nearest_landmark: String(row?.nearest_landmark || \"\").trim(),",
      "12626:        const id = String(ol?.id || \"\").trim();",
      "12629:        const sl = String(ol?.sl_id || \"\").trim();",
      "12734:        const rawId = String(row?.incident_id || \"\").trim();",
      "12737:        const id = String(normalizedId || \"\").trim();",
      "12745:        const rawId = String(row?.incident_id || \"\").trim();",
      "12748:        const id = String(normalizedId || \"\").trim();",
      "12769:          if (String(a.action || \"\").toLowerCase() !== \"fix\") continue;",
      "12782:          const actorNameFallback = actorEmail ? String(actorEmail).split(\"@\")[0] : \"\";",
      "12828:    const viewerUserId = String(session?.user?.id || \"\").trim();",
      "12847:              const incidentId = String(row?.incident_id || \"\").trim();",
      "12863:              const incidentId = String(row?.incident_id || \"\").trim();",
      "12910:        const incidentId = String(r?.light_id || \"\").trim();",
      "12912:          const nextIso = r?.created_at ? String(r.created_at) : new Date(incoming.ts || Date.now()).toISOString();",
      "12916:            const prevIso = String(prev?.[key]?.last_changed_at || \"\");",
      "12968:          const actorNameFallback = actorEmail ? String(actorEmail).split(\"@\")[0] : \"\";",
      "12984:            const sameId = incoming.action_id && x?.action_id && String(x.action_id) === String(incoming.action_id);",
      "12988:              String(x?.action || \"\").toLowerCase() === String(incoming.action || \"\").toLowerCase()",
      "12990:              && String(x?.note || \"\") === String(incoming.note || \"\")",
      "12991:              && String(x?.actor_user_id || \"\") === String(incoming.actor_user_id || \"\")",
      "12999:        const incidentId = String(a?.light_id || \"\").trim();",
      "13000:        const t = String(a.action || \"\").toLowerCase();",
      "13020:          const nextIso = a?.created_at ? String(a.created_at) : new Date(ts || Date.now()).toISOString();",
      "13024:            const prevIso = String(prev?.[key]?.last_changed_at || \"\");",
      "13051:            console.warn(\"[official_lights realtime] invalid row, ignoring:\", row);",
      "13139:        const pid = String(r?.pothole_id || \"\").trim();",
      "13141:          const nextIso = r?.created_at ? String(r.created_at) : new Date(incoming.ts || Date.now()).toISOString();",
      "13145:            const prevIso = String(prev?.[key]?.last_changed_at || \"\");",
      "13161:        const eventType = String(payload?.eventType || \"\").toUpperCase();",
      "13163:          const incidentId = String(payload?.old?.incident_id || \"\").trim();",
      "13175:        const incidentId = String(row?.incident_id || \"\").trim();",
      "13180:            wd_id: String(row?.wd_id || \"\").trim(),",
      "13181:            issue_type: String(row?.issue_type || \"\").trim().toLowerCase(),",
      "13184:            nearest_address: String(row?.nearest_address || \"\").trim(),",
      "13185:            nearest_cross_street: String(row?.nearest_cross_street || \"\").trim(),",
      "13186:            nearest_landmark: String(row?.nearest_landmark || \"\").trim(),",
      "13206:        const eventType = String(payload?.eventType || \"\").toUpperCase();",
      "13207:        const incidentId = String((eventType === \"DELETE\" ? oldRow?.incident_id : row?.incident_id) || \"\").trim();",
      "13208:        const stateLower = String(row?.state || \"\").trim().toLowerCase();",
      "13209:        const changedAtIso = String(row?.last_changed_at || \"\").trim();",
      "13223:              state: String(row?.state || \"\").trim(),",
      "13274:  const officialSignIdSet = useMemo(() => new Set((officialSigns || []).map((s) => String(s?.id || \"\").trim()).filter(Boolean)), [officialSigns]);",
      "13317:    () => (cityLimitPolygons || []).map((poly) => poly?.[0]).filter((ring) => Array.isArray(ring) && ring.length >= 3),",
      "13324:      .map((ring) => orientRing(ring, \"counterclockwise\"))",
      "13325:      .filter((ring) => ring.length >= 3);",
      "13333:  const cityBoundaryBorderColor = /^#[0-9a-fA-F]{6}$/.test(String(tenantMapFeatures?.boundary_border_color || \"\").trim())",
      "13334:    ? String(tenantMapFeatures?.boundary_border_color || \"\").trim().toLowerCase()",
      "13411:      if (!String(lid || \"\").startsWith(\"pothole:\")) continue;",
      "13412:      const pid = String(lid).slice(\"pothole:\".length).trim();",
      "13415:        if (String(a?.action || \"\").toLowerCase() !== \"fix\") continue;",
      "13424:      const keyStr = String(key || \"\");",
      "13429:      const state = String(snap?.state || \"\").trim().toLowerCase();",
      "13433:      const ts = Date.parse(String(snap?.last_changed_at || \"\")) || 0;",
      "13473:        const lid = String(light.id || \"\").trim();",
      "13485:        const pid = String(p?.id || \"\").trim();",
      "13490:        const openCount = (potholeReports || []).filter((r) => String(r?.pothole_id || \"\").trim() === pid && Number(r?.ts || 0) > lastFixTs).length;",
      "13500:        const incidentId = String(r?.light_id || \"\").trim();",
      "13512:        if (dist <= radius) ids.push(String(x.incidentId || \"\").trim());",
      "13538:          id: String(p.ph_id || \"\").trim() || makePotholeIdFromCoords(Number(p.lat), Number(p.lng)),",
      "13544:          lastFixTs: Number(potholeLastFixById?.[String(p.id).trim()] || 0),",
      "13564:        const id = String(s?.id || \"\").trim();",
      "13569:          sign_type: String(s?.sign_type || \"other\").trim().toLowerCase() || \"other\",",
      "13578:        const lid = String(r?.light_id || \"\").trim();",
      "13598:            light_id: String(r?.light_id || \"\").trim() || lightIdFor(Number(r.lat), Number(r.lng)),",
      "13603:          const fallbackIncidentId = String(c?.lightId || lightIdFor(Number(c?.lat || 0), Number(c?.lng || 0))).trim();",
      "13608:            (max, incidentId) => Math.max(max, Number(lastFixByLightId?.[String(incidentId || \"\").trim()] || 0)),",
      "13612:            const rowIncidentId = String(r?.light_id || canonicalIncidentId).trim();",
      "13637:      const lid = String(r.light_id || `${lat.toFixed(5)}:${lng.toFixed(5)}`).trim();",
      "13671:      const id = String(lid || \"\").trim();",
      "13687:      const lid = String(lidRaw || \"\").trim();",
      "13700:        const kind = String(a?.action || \"\").trim().toLowerCase();",
      "13708:        if (String(r?.light_id || \"\").trim() !== lid) continue;",
      "13744:      const pid = String(r?.pothole_id || \"\").trim();",
      "13756:      const incidentId = String(m?.id || \"\").trim();",
      "13774:            ringColor: \"#fff\",",
      "13791:          const incidentId = String(m?.id || \"\").trim();",
      "13798:              ringColor: userReported ? \"#1e88e5\" : \"#fff\",",
      "13810:            ringColor: userReported ? \"#1e88e5\" : \"#fff\",",
      "13830:        const pid = String(m?.pothole_id || \"\").trim();",
      "13838:            ringColor: userReported ? \"#1e88e5\" : \"#fff\",",
      "13849:          ringColor: userReported ? \"#1e88e5\" : \"#fff\",",
      "13903:    const lid = String(selected?.id || \"\").trim();",
      "13907:      nearestAddress: String(selected?.nearest_address || \"\").trim() || \"Address unavailable\",",
      "13909:      nearestCrossStreet: String(selected?.nearest_cross_street || \"\").trim(),",
      "13911:      nearestLandmark: String(selected?.nearest_landmark || \"\").trim(),",
      "13922:    const raw = String(streetlightUtilityContext?.nearestAddress || \"\").trim();",
      "13944:    const houseNumber = String(line1Match?.[1] || \"\").trim();",
      "13945:    const street = String(line1Match?.[2] || line1 || \"\").trim() || \"Address unavailable\";",
      "13965:    const lid = String(light?.id || \"\").trim();",
      "13970:    const fromDb = (officialLights || []).find((x) => String(x?.id || \"\").trim() === lid) || light;",
      "13971:    const existingAddress = String(fromDb?.nearest_address || \"\").trim();",
      "13972:    const existingCrossStreet = String(fromDb?.nearest_cross_street || \"\").trim();",
      "13973:    const existingLandmark = String(fromDb?.nearest_landmark || \"\").trim();",
      "13996:      const nearestAddress = String(geo?.nearestAddress || \"\").trim();",
      "13997:      const nearestStreet = String(geo?.nearestStreet || \"\").trim();",
      "13998:      const nearestCrossStreet = String(geo?.nearestCrossStreet || \"\").trim();",
      "13999:      const nearestLandmark = String(geo?.nearestLandmark || \"\").trim();",
      "14000:      const nearestIntersection = String(geo?.nearestIntersection || \"\").trim();",
      "14018:          if (String(row?.id || \"\").trim() !== lid) return row;",
      "14059:    const signId = String(selectedDomainMarker?.id || \"\").trim();",
      "14066:    const pid = String(selectedDomainMarker?.pothole_id || \"\").trim();",
      "14070:      .filter((r) => String(r?.pothole_id || \"\").trim() === pid)",
      "14077:      String(selectedDomainMarker?._geoNearestAddress || \"\").trim() ||",
      "14078:      String(selectedDomainMarker?.location_label || \"\").trim() ||",
      "14084:    const resolvedLandmark = String(selectedDomainMarker?._geoNearestLandmark || \"\").trim() || landmarkFromNote;",
      "14090:      String(lifecycleSnapshot?.state || \"\").trim() ||",
      "14093:      String(lifecycleSnapshot?.last_changed_at || \"\").trim() ||",
      "14094:      (latestReportTs ? new Date(latestReportTs).toISOString() : \"\");",
      "14105:    const markerIncidentId = String(selectedDomainMarker?.id || \"\").trim();",
      "14110:      markerIncidentId || String(selectedDomainMarker?.incident_ids?.[0] || \"\").trim()",
      "14117:      (max, id) => Math.max(max, Number(lastFixByLightId?.[String(id || \"\").trim()] || 0)),",
      "14121:      const rowIncidentId = String(r?.light_id || incidentId).trim();",
      "14129:      let t = String(r?.type || r?.report_type || \"\").trim().toLowerCase() || \"other\";",
      "14153:      String(cached?.nearest_address || \"\").trim() ||",
      "14154:      String(selectedDomainMarker?._geoNearestAddress || \"\").trim() ||",
      "14155:      String(selectedDomainMarker?.location_label || \"\").trim() ||",
      "14162:      String(cached?.nearest_landmark || \"\").trim() ||",
      "14163:      String(selectedDomainMarker?._geoNearestLandmark || \"\").trim() ||",
      "14169:        ts: Date.parse(String(entry?.snap?.last_changed_at || \"\")) || 0,",
      "14175:      String(lifecycleSnapshot?.state || \"\").trim() ||",
      "14178:      String(lifecycleSnapshot?.last_changed_at || \"\").trim() ||",
      "14179:      (latestTs ? new Date(latestTs).toISOString() : \"\") ||",
      "14180:      (maxLastFixTs ? new Date(maxLastFixTs).toISOString() : \"\");",
      "14181:    const displayId = String(cached?.wd_id || \"\").trim() || makeWaterDrainIdFromIncidentId(incidentId);",
      "14185:      issueLabel: formatWaterDrainIssueLabel(String(cached?.issue_type || \"\").trim() || issueType),",
      "14267:        const lid = String(r?.light_id || \"\").trim();",
      "14346:        if (String(a.action || \"\").toLowerCase() !== \"working\") continue;",
      "14406:        if (String(a.action || \"\").toLowerCase() !== \"working\") continue;",
      "14465:    const mode = String(streetlightInViewFilterMode || \"\").trim().toLowerCase();",
      "14467:    const lid = String(lightIdRaw || \"\").trim();",
      "14510:    const domainKey = String(adminReportDomain || \"\").trim();",
      "14519:    const markerId = String(marker?.id || marker?.pothole_id || \"\").trim();",
      "14520:    const markerLightId = String(marker?.lightId || \"\").trim();",
      "14556:      const potholeById = new Map((potholes || []).map((p) => [String(p?.id || \"\").trim(), p]));",
      "14560:        const pid = String(r?.pothole_id || \"\").trim();",
      "14577:          displayId: String(p?.ph_id || \"\").trim() || pid,",
      "14631:      const lightId = String(g?.lightId || \"\").trim();",
      "14633:        ? String(lightId || \"\").replace(/^potholes:/i, \"pothole:\")",
      "14662:            const incidentId = String(r?.light_id || \"\").trim();",
      "14672:            const incidentId = String(r?.light_id || \"\").trim();",
      "14683:                const s = (officialSigns || []).find((x) => String(x?.id || \"\").trim() === incidentId);",
      "14704:            const pid = String(m?.pothole_id || \"\").trim();",
      "14707:              .filter((r) => String(r?.pothole_id || \"\").trim() === pid)",
      "14719:              display_id: String(m?.id || \"\").trim(),",
      "14720:              location_label: String(m?.location_label || \"\").trim(),",
      "14731:            light_id: String(r?.light_id || \"\").trim() || lightIdFor(Number(r.lat), Number(r.lng)),",
      "14736:            const incidentId = String(c?.lightId || lightIdFor(Number(c?.lat || 0), Number(c?.lng || 0))).trim();",
      "14738:            const displayId = String(selectedWaterDrainInfo?.displayId || \"\").trim() || makeWaterDrainIdFromIncidentId(incidentId);",
      "14761:          incident_id: String(m?.id || \"\").trim(),",
      "14769:            .filter((r) => String(r?.light_id || \"\").trim() === String(m?.id || \"\").trim())",
      "14771:          display_id: String(m?.id || \"\").trim(),",
      "14777:          incident_id: String(m?.id || \"\").trim(),",
      "14785:            .filter((r) => String(r?.light_id || \"\").trim() === String(m?.id || \"\").trim())",
      "14787:          display_id: String(m?.id || \"\").trim(),",
      "14824:        const lid = String(l?.id || \"\").trim();",
      "14829:        const markerColor = String(officialMarkerColorForViewer(lid) || \"\").trim().toLowerCase();",
      "14859:      const lid = String(l?.id || \"\").trim();",
      "14894:      const lid = String(l?.id || \"\").trim();",
      "14901:    const selectedId = String(selectedOfficialId || \"\").trim();",
      "14903:    const stillVisible = (visibleOfficialLights || []).some((l) => String(l?.id || \"\").trim() === selectedId);",
      "14910:        lightId: String(g?.display_id || g?.incident_id || \"\").trim(),",
      "14911:        incidentId: String(g?.incident_id || \"\").trim(),",
      "14917:        location_label: String(g?.location_label || \"\").trim(),",
      "14918:        domain: String(g?.domain || adminReportDomain || \"\").trim(),",
      "14921:        if (String(g?.domain || \"\").trim() !== \"streetlights\") return true;",
      "14922:        const incidentId = String(g?.incidentId || \"\").trim();",
      "14926:        const domain = String(g?.domain || \"\").trim();",
      "14953:    // Accept: ms number, ISO string, Date, or seconds",
      "14958:    if (typeof t === \"string\") {",
      "14976:      return new Date(ms).toLocaleString();",
      "14989:    const key = String(t || \"\").toLowerCase().trim();",
      "14992:      flicker: \"Flickering\",",
      "14993:      on_day: \"On during day\",",
      "15027:      const t = String(r.type || r.report_type || \"\").trim() || \"unknown\";",
      "15069:    // and ignoring quick subsequent clicks if you have logic for that.",
      "15089:    const mode = String(options?.mode || \"full\").trim().toLowerCase();",
      "15126:        return String(ranked?.formatted_address || \"\").trim();",
      "15146:          const routeName = String(routeComp?.long_name || \"\").trim();",
      "15156:      const normalizeRoad = (v) => String(v || \"\").toLowerCase().replace(/[^a-z0-9]/g, \"\");",
      "15186:            const routeName = String(routeComp?.long_name || \"\").trim();",
      "15202:        return String(nearest?.name || \"\").trim();",
      "15208:    const bearingDegrees = (fromLat, fromLng, toLat, toLng) => {",
      "15222:    const directionFromBearing = (deg) => {",
      "15234:        return String(routeComp?.long_name || \"\").trim();",
      "15243:          const nm = String(c?.long_name || \"\").trim();",
      "15291:            const routeName = String(routeComp?.long_name || \"\").trim();",
      "15309:            const label = String(r?.formatted_address || \"\").trim();",
      "15326:              (c.routes || []).some((nm) => String(nm || \"\").toLowerCase() === preferredKey)",
      "15335:              (nm) => String(nm || \"\").toLowerCase() !== String(preferredRoute || \"\").toLowerCase()",
      "15360:                  (nm) => String(nm || \"\").toLowerCase() === String(preferredRoute || \"\").toLowerCase()",
      "15388:          const dir = directionFromBearing(",
      "15389:            bearingDegrees(nearest.lat, nearest.lng, qlat, qlng)",
      "15429:            const routeName = String(routeComp?.long_name || \"\").trim();",
      "15456:        const dir = directionFromBearing(",
      "15457:          bearingDegrees(midpointLat, midpointLng, qlat, qlng)",
      "15467:        const v = String(raw || \"\").trim();",
      "15522:                const nm = String(r?.name || \"\").trim();",
      "15563:                .map((r) => String(r?.name || \"\").trim())",
      "15586:          const compName = String(namedComp?.long_name || \"\").trim();",
      "15588:          const formatted = String(r?.formatted_address || \"\").trim();",
      "15594:        const msg = String(e?.message || e || \"\").toLowerCase();",
      "15635:              const nearestAddress = String(nearestAddressRaw || \"\").trim();",
      "15636:              const nearestStreet = String(nearestStreetRaw || \"\").trim();",
      "15640:              const nearestLandmark = String(nearestLandmarkRaw || \"\").trim();",
      "15641:              const nearestIntersection = String(nearestIntersectionRaw || \"\").trim();",
      "15714:            const maybe = String(r?.formatted_address || \"\").trim();",
      "15727:        const nearestStreet = String(nearestStreetRaw || \"\").trim();",
      "15731:        const nearestLandmark = String(nearestLandmarkRaw || \"\").trim();",
      "15732:        const nearestIntersection = String(nearestIntersectionRaw || \"\").trim();",
      "15755:        const nearestStreet = String(nearestStreetRaw || \"\").trim();",
      "15759:        const nearestLandmark = String(nearestLandmarkRaw || \"\").trim();",
      "15760:        const nearestIntersection = String(nearestIntersectionRaw || \"\").trim();",
      "15836:          potholeCode: String(potholeCode || \"\").trim(),",
      "15837:          reportNumber: String(reportNumber || \"\").trim(),",
      "15838:          notes: String(notes || \"\").trim(),",
      "15844:          closestAddress: String(closestAddress || \"\").trim() || \"Address unavailable\",",
      "15845:          closestLandmark: String(closestLandmark || \"\").trim() || \"No nearby landmark\",",
      "15846:          closestCrossStreet: String(closestCrossStreet || \"\").trim() || \"No nearby cross street\",",
      "15847:          closestIntersection: String(closestIntersection || \"\").trim() || \"No nearby intersection\",",
      "15848:          submittedAtIso: String(submittedAtIso || new Date().toISOString()),",
      "15849:          submittedAtLocal: new Date(submittedAtIso || Date.now()).toLocaleString(),",
      "15853:            name: String(reporter?.name || \"\").trim() || \"Unknown\",",
      "15854:            email: String(reporter?.email || \"\").trim() || \"Not provided\",",
      "15855:            phone: String(reporter?.phone || \"\").trim() || \"Not provided\",",
      "15861:        return { ok: false, reason: String(error?.message || \"invoke_error\").trim() || \"invoke_error\", skipped: false };",
      "15865:      const reason = String(data?.reason || \"\").trim();",
      "15876:      return { ok: false, reason: String(e?.message || \"invoke_failed\"), skipped: false };",
      "15899:          issueType: String(issueTypeLabel || \"\").trim() || \"Water / Drain Issue\",",
      "15900:          reportNumber: String(reportNumber || \"\").trim(),",
      "15901:          notes: String(notes || \"\").trim(),",
      "15907:          closestAddress: String(closestAddress || \"\").trim() || \"Address unavailable\",",
      "15908:          closestLandmark: String(closestLandmark || \"\").trim() || \"No nearby landmark\",",
      "15909:          closestCrossStreet: String(closestCrossStreet || \"\").trim() || \"No nearby cross street\",",
      "15910:          closestIntersection: String(closestIntersection || \"\").trim() || \"No nearby intersection\",",
      "15911:          submittedAtIso: String(submittedAtIso || new Date().toISOString()),",
      "15912:          submittedAtLocal: new Date(submittedAtIso || Date.now()).toLocaleString(),",
      "15916:            name: String(reporter?.name || \"\").trim() || \"Unknown\",",
      "15917:            email: String(reporter?.email || \"\").trim() || \"Not provided\",",
      "15918:            phone: String(reporter?.phone || \"\").trim() || \"Not provided\",",
      "15924:        return { ok: false, reason: String(error?.message || \"invoke_error\").trim() || \"invoke_error\", skipped: false };",
      "15929:      const reason = String(data?.reason || \"\").trim();",
      "15940:      return { ok: false, reason: String(e?.message || \"invoke_failed\"), skipped: false };",
      "15945:    const label = String(domainLabel || \"Report\").trim();",
      "15947:    const reason = String(noticeRes?.reason || \"\").trim();",
      "15962:    const rand = Math.random().toString(36).slice(2, 9);",
      "15963:    const keyHint = String(reportKeyHint || \"\").replace(/[^a-zA-Z0-9_-]/g, \"\").slice(0, 32);",
      "15964:    const path = `${tenantKey}/${domain}/${new Date().toISOString().slice(0, 10)}/${ts}_${keyHint || \"report\"}_${rand}.${ext}`;",
      "15972:    return String(data?.publicUrl || \"\").trim();",
      "16012:      const resolvedIncidentId = String(near?.marker?.id || \"\").trim();",
      "16013:      if (resolvedIncidentId && resolvedIncidentId !== String(targetRaw?.lightId || \"\").trim()) {",
      "16018:            String(near?.marker?.location_label || \"\").trim()",
      "16019:            || String(targetRaw?.locationLabel || \"\").trim(),",
      "16127:        String(submitGeo.nearestAddress || target.nearestAddress || \"\").trim() || \"Address unavailable\";",
      "16128:      const potholeLandmark = String(submitGeo.nearestLandmark || target.nearestLandmark || \"\").trim();",
      "16129:      const potholeCrossStreet = String(submitGeo.nearestCrossStreet || target.nearestCrossStreet || \"\").trim();",
      "16130:      const potholeIntersection = String(submitGeo.nearestIntersection || target.nearestIntersection || \"\").trim();",
      "16141:        const potholeIncidentId = `pothole:${String(potholeId || \"\").trim()}`;",
      "16209:            if (String(row?.id || \"\").trim() !== String(potholeId || \"\").trim()) return row;",
      "16262:      const submittedAtIso = insReport.data?.created_at || new Date().toISOString();",
      "16288:        ? String(target?.lightId || \"\").trim()",
      "16315:      const waterNearestAddress = String(waterSubmitGeo?.nearestAddress || target.nearestAddress || \"\").trim();",
      "16316:      const waterNearestLandmark = String(waterSubmitGeo?.nearestLandmark || target.nearestLandmark || \"\").trim();",
      "16317:      const waterNearestCrossStreet = String(waterSubmitGeo?.nearestCrossStreet || target.nearestCrossStreet || \"\").trim();",
      "16318:      const waterNearestIntersection = String(waterSubmitGeo?.nearestIntersection || target.nearestIntersection || \"\").trim();",
      "16331:        ? `Sign type: ${String(target.signType || \"\").trim() || \"Unknown\"}`",
      "16334:        ? String(domainReportIssue || WATER_DRAIN_ISSUE_OPTIONS[0].value).trim().toLowerCase()",
      "16357:          : (String(target.lightId || \"\").trim() || `${isWaterDrainTarget ? \"water_main\" : target.domain}:${target.lat.toFixed(5)}:${target.lng.toFixed(5)}`),",
      "16391:        const nearestAddress = String(waterNearestAddress || \"\").trim();",
      "16392:        const nearestCrossStreet = String(waterNearestCrossStreet || \"\").trim();",
      "16393:        const nearestLandmark = String(waterNearestLandmark || \"\").trim();",
      "16400:                incident_id: String(saved.light_id || \"\").trim(),",
      "16413:            const incidentKey = String(saved.light_id || \"\").trim();",
      "16418:                  wd_id: String(cacheData?.row?.wd_id || \"\").trim() || makeWaterDrainIdFromIncidentId(incidentKey),",
      "16425:                  geo_updated_at: new Date().toISOString(),",
      "16426:                  updated_at: new Date().toISOString(),",
      "16439:        const submittedAtIso = data?.created_at || new Date().toISOString();",
      "16440:        const nearestAddress = String(waterNearestAddress || \"\").trim() || \"Address unavailable\";",
      "16441:        const nearestLandmark = String(waterNearestLandmark || \"\").trim() || \"No nearby landmark\";",
      "16442:        const nearestCrossStreet = String(waterNearestCrossStreet || \"\").trim() || \"No nearby cross street\";",
      "16443:        const nearestIntersection = String(waterNearestIntersection || \"\").trim() || \"No nearby intersection\";",
      "16488:  const msg = String(err?.message || \"\").toLowerCase();",
      "16502:      .map((v) => String(v || \"\").trim())",
      "16530:        if (insErr && String(insErr.message || \"\").toLowerCase().includes(\"report_quality\")) {",
      "16542:        if (plain.error && String(plain.error.message || \"\").toLowerCase().includes(\"report_quality\")) {",
      "16549:              id: `local_${Date.now()}_${Math.random().toString(16).slice(2)}`,",
      "16550:              created_at: new Date().toISOString(),",
      "16564:        if (plain.error && String(plain.error.message || \"\").toLowerCase().includes(\"report_quality\")) {",
      "16571:              id: `local_${Date.now()}_${Math.random().toString(16).slice(2)}`,",
      "16572:              created_at: new Date().toISOString(),",
      "16670:            id: `local_${Date.now()}_${Math.random().toString(16).slice(2)}`,",
      "16671:            created_at: new Date().toISOString(),",
      "16683:              id: `local_${Date.now()}_${Math.random().toString(16).slice(2)}`,",
      "16684:              created_at: new Date().toISOString(),",
      "16826:          const existing = (officialLights || []).find((x) => String(x?.id || \"\").trim() === String(lightId || \"\").trim());",
      "16828:            Boolean(String(existing?.nearest_address || \"\").trim()) ||",
      "16829:            Boolean(String(existing?.nearest_cross_street || \"\").trim()) ||",
      "16830:            Boolean(String(existing?.nearest_landmark || \"\").trim());",
      "16834:              const nearestAddress = String(geo?.nearestAddress || \"\").trim();",
      "16835:              const nearestCrossStreet = String(geo?.nearestCrossStreet || \"\").trim();",
      "16836:              const nearestLandmark = String(geo?.nearestLandmark || \"\").trim();",
      "16853:                  if (String(row?.id || \"\").trim() !== String(lightId || \"\").trim()) return row;",
      "17003:          sign_type: String(q.sign_type || \"other\").trim().toLowerCase() || \"other\",",
      "17025:        const slIds = uniqueLightRows.map((r) => String(r.sl_id || \"\").trim()).filter(Boolean);",
      "17040:          const existingSlSet = new Set(existingLights.map((x) => String(x?.sl_id || \"\").trim()).filter(Boolean));",
      "17042:          const rowsToInsert = uniqueLightRows.filter((r) => !existingSlSet.has(String(r.sl_id || \"\").trim()));",
      "17375:    const uniqueIds = Array.from(new Set((ids || []).map((x) => String(x || \"\").trim()).filter(Boolean)));",
      "17388:    setOfficialLights((prev) => prev.filter((x) => !idSet.has(String(x?.id || \"\").trim())));",
      "17389:    if (selectedOfficialId && idSet.has(String(selectedOfficialId || \"\").trim())) {",
      "17417:    const uniqueIds = Array.from(new Set((ids || []).map((x) => String(x || \"\").trim()).filter(Boolean)));",
      "17422:    const noteClean = String(noteText || \"\").trim();",
      "17430:          const pid = String(id.slice(\"pothole:\".length) || \"\").trim();",
      "17432:          const pothole = (potholes || []).find((p) => String(p?.id || \"\").trim() === pid);",
      "17438:              ph_id: String(pothole?.ph_id || \"\").trim() || makePotholeIdFromCoords(Number(pothole?.lat || 0), Number(pothole?.lng || 0)),",
      "17502:    const lid = String(lightId || \"\").trim();",
      "17524:    const noteClean = String(noteText || \"\").trim();",
      "17554:    const fixIso = newest?.iso || new Date().toISOString();",
      "17559:    const allOfficialLights = ids.every((id) => officialIdSet.has(String(id || \"\").trim()));",
      "17586:      const nextIso = new Date(fixMs).toISOString();",
      "17605:            String(profile?.full_name || \"\").trim() ||",
      "17606:            String(session?.user?.user_metadata?.full_name || \"\").trim() ||",
      "17607:            String(session?.user?.email || \"\").split(\"@\")[0] ||",
      "17616:    const idForMessage = String(ids?.[0] || \"\").trim();",
      "17631:    const noteClean = String(noteText || \"\").trim();",
      "17688:            String(profile?.full_name || \"\").trim() ||",
      "17689:            String(session?.user?.user_metadata?.full_name || \"\").trim() ||",
      "17690:            String(session?.user?.email || \"\").split(\"@\")[0] ||",
      "17701:      const tsIso = new Date().toISOString();",
      "17714:    const lid = String(lightId || \"\").trim();",
      "17715:    const uid = String(session?.user?.id || \"\").trim();",
      "17729:          reported_at: new Date().toISOString(),",
      "17744:    const lid = String(lightId || \"\").trim();",
      "17745:    const uid = String(session?.user?.id || \"\").trim();",
      "18097:            if (movedMeters >= 4) heading = bearingBetween(prevPos, nextPos);",
      "18220:  // ✅ Load Google Maps JS before rendering <GoogleMap />",
      "18608:    const activeHost = typeof window !== \"undefined\" ? String(window.location.host || \"\").trim() : \"\";",
      "18954:            name: String(guestInfoDraft?.name || \"\"),",
      "18955:            phone: String(guestInfoDraft?.phone || \"\"),",
      "18956:            email: String(guestInfoDraft?.email || \"\"),",
      "18995:          const pendingKind = String(pendingGuestAction?.kind || \"\").trim().toLowerCase();",
      "19161:            Place or clear queued assets before switching to <b>{String(pendingDomainSwitchTarget?.label || \"the selected domain\")}</b>.",
      "19232:              value={String(pendingQueuedSign?.sign_type || \"\")}",
      "19236:                    ? { ...prev, sign_type: String(e.target.value || \"\").trim().toLowerCase() }",
      "19265:              disabled={!STREET_SIGN_TYPE_VALUES.has(String(pendingQueuedSign?.sign_type || \"\").trim().toLowerCase())}",
      "19273:                cursor: STREET_SIGN_TYPE_VALUES.has(String(pendingQueuedSign?.sign_type || \"\").trim().toLowerCase()) ? \"pointer\" : \"not-allowed\",",
      "19274:                opacity: STREET_SIGN_TYPE_VALUES.has(String(pendingQueuedSign?.sign_type || \"\").trim().toLowerCase()) ? 1 : 0.6,",
      "19303:            If you need to report damage to your vehicle from a pothole, you will need to file a police report as soon as possible, then bring this information to the City Manager's Office.",
      "19462:                ? \"Why is this being re-opened? (new damage, recurring issue, etc.)\"",
      "19483:                const noteText = String(markFixedNote || \"\").trim();",
      "19493:                    const pid = String(potholeMarker?.pothole_id || \"\").trim();",
      "19561:                await markIncidentsFixedByIds(deleteCircleCandidateIds, String(deleteCircleNote || \"\").trim());",
      "19761:          const incidentKey = String(incidentId || \"\").trim();",
      "19765:            const pid = String(incidentKey.slice(\"pothole:\".length) || \"\").trim();",
      "19767:            const m = (potholes || []).find((p) => String(p?.id || \"\").trim() === pid);",
      "19772:              ph_id: String(m?.ph_id || \"\").trim() || makePotholeIdFromCoords(Number(m?.lat || 0), Number(m?.lng || 0)),",
      "20029:                const targetToken = `pothole:${Date.now()}:${Math.random().toString(36).slice(2, 8)}`;",
      "20066:              const resolvedWaterIncidentId = String(nearestWaterMarker?.marker?.id || \"\").trim();",
      "20082:                  String(nearestWaterMarker?.marker?.location_label || \"\").trim()",
      "20095:              // Geocoding runs only during explicit submit flows.",
      "20118:        {showCityBoundaryBorder && cityBoundaryOuterRings.map((ring, idx) => (",
      "20121:            paths={ring}",
      "20144:          getMarkerRingColor={officialMarkerRingColorForViewer}",
      "20173:                    m.ringColor || \"#fff\",",
      "20306:              const lid = String(selectedOfficialLightForPopup?.id || \"\").trim();",
      "20331:              const lid = String(selectedOfficialLightForPopup?.id || \"\").trim();",
      "20372:                        const onStreet = String(streetlightUtilityContext.nearestStreet || \"\").trim();",
      "20373:                        const intersectionRaw = String(streetlightUtilityContext.nearestIntersection || \"\").trim();",
      "20374:                        const nearestCrossStreetRaw = String(streetlightUtilityContext.nearestCrossStreet || \"\").trim();",
      "20375:                        const normalizeRoad = (v) => String(v || \"\").toLowerCase().replace(/[^a-z0-9]/g, \"\");",
      "20386:                            .map((x) => String(x || \"\").trim())",
      "20557:              const locationLabelRaw = String(selectedPotholeInfo?.locationLabel || \"\").trim();",
      "20559:              const nearestLandmark = String(nearMatch?.[1] || \"\").trim();",
      "20606:                  {String(selectedPotholeInfo?.lastChangedAt || \"\").trim()",
      "20620:                    const pid = String(selectedDomainMarker?.pothole_id || \"\").trim();",
      "20632:                    const pid = String(selectedDomainMarker?.pothole_id || \"\").trim();",
      "20640:              const pid = String(selectedDomainMarker?.pothole_id || \"\").trim();",
      "20724:              const incidentId = String(selectedWaterDrainInfo?.incidentId || selectedDomainMarker?.id || \"\").trim();",
      "20725:              const displayId = String(selectedWaterDrainInfo?.displayId || \"\").trim() || makeWaterDrainIdFromIncidentId(incidentId);",
      "20732:              const locationLabelRaw = String(selectedWaterDrainInfo?.locationLabel || \"\").trim();",
      "20734:              const nearestLandmark = String(nearMatch?.[1] || \"\").trim();",
      "20774:              const incidentId = String(selectedDomainMarker?.id || \"\").trim();",
      "20789:                    {String(selectedWaterDrainInfo?.lastChangedAt || \"\").trim()",
      "20819:              const incidentId = String(selectedWaterDrainInfo?.incidentId || selectedDomainMarker?.id || \"\").trim();",
      "20898:              Sign ID: {String(selectedDomainMarker.id || \"\").slice(0, 8) || \"Sign\"}",
      "20904:              const signId = String(selectedDomainMarker.id || \"\").trim();",
      "20908:              const lifecycleState = String(selectedStreetSignLifecycleSnapshot?.state || \"\").trim();",
      "20909:              const lifecycleChangedAt = String(selectedStreetSignLifecycleSnapshot?.last_changed_at || \"\").trim();",
      "20984:                  lightId: String(selectedDomainMarker.id || \"\").trim(),",
      "21073:                  value={String(selectedQueuedLightForPopup.sign_type || \"other\")}"
    ],
    "bulk_selection_logic": [
      "100:const BULK_MAX_LIGHTS_PER_SUBMIT = 10;",
      "1959:  bulkMode,",
      "1960:  bulkSelectedSet,",
      "1967:    const isSelected = bulkMode && bulkSelectedSet.has(ol.id);",
      "1988:  bulkMode,",
      "1989:  bulkSelectedSet,",
      "2000:    bulkMode,",
      "2001:    bulkSelectedSet,",
      "2006:  latestRef.current = { show, lights, bulkMode, bulkSelectedSet, getMarkerColor, getMarkerRingColor };",
      "2071:      const isSelected = state.bulkMode && state.bulkSelectedSet?.has?.(ol.id);",
      "2190:  }, [drawOverlayCanvas, show, lights, bulkMode, bulkSelectedSet, getMarkerColor, getMarkerRingColor]);",
      "10247:      if (!has && prev.length >= BULK_MAX_LIGHTS_PER_SUBMIT) {",
      "10248:        openNotice(\"⚠️\", \"Selection limit\", `You can select up to ${BULK_MAX_LIGHTS_PER_SUBMIT} lights per bulk report.`);",
      "11211:  const [bulkMode, setBulkMode] = useState(false);",
      "11212:  const [bulkSelectedIds, setBulkSelectedIds] = useState([]); // array of official light UUIDs",
      "11238:  const bulkSelectedSet = useMemo(() => new Set(bulkSelectedIds), [bulkSelectedIds]);",
      "11239:  const bulkSelectedCount = bulkSelectedIds.length;",
      "11241:  function clearBulkSelection() {",
      "11247:    const alreadySelected = bulkSelectedSet.has(id);",
      "11254:      if (prev.length >= BULK_MAX_LIGHTS_PER_SUBMIT) {",
      "11255:        openNotice(\"⚠️\", \"Selection limit\", `You can select up to ${BULK_MAX_LIGHTS_PER_SUBMIT} lights per bulk report.`);",
      "11265:    clearBulkSelection();",
      "11365:    if (bulkMode && mappingMode) {",
      "11369:  }, [bulkMode, mappingMode]);",
      "13872:    if (bulkMode || !isStreetlightsDomain) return null;",
      "13876:  }, [bulkMode, isStreetlightsDomain, selectedOfficialId, officialLights]);",
      "14226:    if (bulkMode || !mappingMode || !isAdmin) return null;",
      "14230:  }, [bulkMode, mappingMode, isAdmin, selectedQueuedTempId, mappingQueue]);",
      "14498:    if (bulkMode) {",
      "14506:  }, [mappingMode, bulkMode, toggleBulkSelect]);",
      "17151:  const ids = bulkSelectedIds;",
      "17153:  if (ids.length > BULK_MAX_LIGHTS_PER_SUBMIT) {",
      "17154:    openNotice(\"⚠️\", \"Selection limit\", `You can submit up to ${BULK_MAX_LIGHTS_PER_SUBMIT} lights in one bulk report.`);",
      "17294:  clearBulkSelection();",
      "18531:    if (bulkMode) {",
      "18534:      clearBulkSelection();",
      "18546:    bulkMode,",
      "18551:    clearBulkSelection,",
      "18564:    if (canUseStreetlightBulk || !bulkMode) return;",
      "18567:    clearBulkSelection();",
      "18568:  }, [canUseStreetlightBulk, bulkMode, clearBulkSelection]);",
      "20141:          bulkMode={bulkMode}",
      "20142:          bulkSelectedSet={bulkSelectedSet}",
      "20230:      {!bulkMode && selectedOfficialLightForPopup && selectedOfficialPopupPixel && (",
      "20511:      {!bulkMode && adminReportDomain === \"potholes\" && selectedDomainMarker && selectedDomainPopupPixel && (",
      "20684:      {!bulkMode && adminReportDomain === \"water_drain_issues\" && selectedDomainMarker && selectedDomainPopupPixel && (",
      "20859:      {!bulkMode && adminReportDomain === \"street_signs\" && selectedDomainMarker && selectedDomainPopupPixel && (",
      "21021:      {!bulkMode && selectedQueuedLightForPopup && selectedQueuedPopupPixel && (",
      "21338:            className={`sl-map-tool-mini sl-bulk-tool-btn ${bulkMode ? \"is-on\" : \"\"}`}",
      "21360:                  clearBulkSelection();",
      "21366:            title={bulkMode ? \"Bulk selection ON\" : \"Bulk selection OFF\"}",
      "21420:                      if (bulkMode) setBulkMode(false);",
      "21459:                          clearBulkSelection();",
      "21512:                          clearBulkSelection();",
      "21911:          {canUseStreetlightBulk && bulkMode && (",
      "21947:                  Max {BULK_MAX_LIGHTS_PER_SUBMIT} lights per bulk report",
      "21954:                    clearBulkSelection();",
      "22499:          {canUseStreetlightBulk && bulkMode && (",
      "22535:                  Max {BULK_MAX_LIGHTS_PER_SUBMIT}",
      "22542:                    clearBulkSelection();"
    ]
  },
  "remaining_manual_checks": {
    "ios_zoom_reliability": "pending device validation (Drive Test checklist)",
    "bulk_selection_persistence": "pending interactive map movement/reload/realtime validation",
    "my_reports_completeness": "pending authenticated user reconciliation against live saved/ring visibility"
  }
}```
